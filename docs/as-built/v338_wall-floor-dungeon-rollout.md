# v338 — Wall-floor dungeon rollout

Extended v308 polish to ceiling/obstacle normals, `room_divider` wall tint, and client bot proof on the `dungeon_levels` descent route (`79_wall_floor_dungeon_rollout.json`).

Verification: `make client-unit`, `godot --headless --path client --script res://tests/test_factories.gd`
