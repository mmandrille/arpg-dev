# v339 — Room-type floor tint

Added `shared/assets/dungeon_room_presentation.v0.json` and client `DungeonRoomFloorTint` overlays partitioned by `room_divider` walls with treasure-chest archetype detection.

Verification: `godot --headless --path client --script res://tests/test_factories.gd`
