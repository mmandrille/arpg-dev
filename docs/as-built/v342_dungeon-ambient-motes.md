# v342 — Dungeon ambient motes

Depth-scaled ambient mote spheres attach to dungeon ground via `DungeonAmbientMotes` and shared presentation config.

Verification: `godot --headless --path client --script res://tests/test_factories.gd`

Visual: `make bot-visual scenario=12_dungeon_levels`
