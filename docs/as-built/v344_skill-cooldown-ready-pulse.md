# v344 — Skill cooldown ready pulse

`skill_bar.gd` flashes the slot and briefly tints the panel border when a local cooldown decays to zero.

Verification: `godot --headless --path client --script res://tests/test_skill_bar.gd`
