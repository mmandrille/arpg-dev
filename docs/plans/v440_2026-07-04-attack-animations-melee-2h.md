# v440 Plan — Attack Animations Melee 2H

- [x] `attack_presentation.v0.json` + loader
- [x] `attack_2h` clip in `build_animations.gd` / `character_anims.tres`
- [x] `CombatLocalAttackPresentation` clip selection from equipped item
- [x] Off-hand visual suppression for 2H weapons

## Verification

```bash
make gen-anims
make client-unit
```
