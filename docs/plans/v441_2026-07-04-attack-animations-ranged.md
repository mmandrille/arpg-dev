# v441 Plan — Attack Animations Ranged

- [x] `attack_ranged` + `attack_staff` clips in `build_animations.gd`
- [x] `make gen-anims` refresh `character_anims.tres`
- [x] Loader routes staff vs bow by item def id

## Verification

```bash
make gen-anims
make client-unit
```
