# arpg-dev - Engineering review at slice **v80** (overview)

**Date:** 2026-06-11
**Reviewer:** Codex engineering review pass
**Scope:** Whole repo - Go authoritative server, Godot thin client, shared contracts, Python tooling, SDD process
**Git baseline:** `main` @ `2acb4c1` - `feat: v79: elite pack roles`; worktree contained in-progress v80 slice changes during review.
**Companion reports:**
- [`backend/20260611_v80-backend.md`](backend/20260611_v80-backend.md) - Go authoritative server
- [`client/20260611_v80-client.md`](client/20260611_v80-client.md) - Godot thin client
- [`extras/20260611_v80-shared-tooling-and-process.md`](extras/20260611_v80-shared-tooling-and-process.md) - contracts, Python tooling, docs/SDD

---

## 1. Executive summary

**Overall score: 8.4 / 10.**

The repo remains healthy through v80. The latest combat work improved moment-to-moment readability
without weakening authority: server events still own combat facts, while the Godot client renders
display-only feedback. Pack generation also matured into structured, data-driven encounters with
role composition and internal elite leaders.

The main risks are the same shape as v70: large coordination files and one small realtime fanout
correctness issue. None of these block gameplay progress, but they are good candidates for bounded
maintenance slices before the next large feature batch.

## 2. Scorecard

| Dimension | Score | Rationale |
|-----------|-------|-----------|
| Architecture (boundaries) | 9.0 | Server authority, shared rules, replay, and thin-client presentation remain intact. |
| Architecture (internal cohesion) | 7.1 | Pack-generation cohesion improved; `main.gd`, `game_test.go`, `run.py`, and `validate_shared.py` are still large. |
| Technical correctness | 8.5 | Focused v80 checks passed; final `make ci` is required before commit. Realtime fanout level reads still merit a fix. |
| Test quality | 8.8 | 43 protocol scenarios, 31 client scenarios, Go/Python/Godot layers, and new threat readability proof. |
| Maintainability | 7.2 | SDD discipline offsets file size, but helper extraction should become routine. |
| Documentation | 8.6 | Specs/plans/as-built/reviews are current; PROGRESS needs v79/v80 status consolidation during close-out. |
| Process / SDD | 9.3 | Autoloop slices produced docs, focused tests, bot proof, and CI gates. |
| Overall | **8.4** | Strong foundation; next wins are small refactors and defensive parsing. |

## 3. Cross-cutting themes

### Theme 1 - Server facts, client readability

The current direction is correct: v80 uses `monster_aggro` as an authoritative fact and only adds a
client-side `AGGRO` presentation. That keeps replay/protocol simple while making combat clearer.

### Theme 2 - Encounters are becoming structured

Pack counts, member radius, roles, assist radius, and elite leader markers now give future combat
slices a better foundation than isolated random monster placement.

### Theme 3 - Large coordination files are still the tax

`client/scripts/main.gd`, `tools/bot/run.py`, `tools/validate_shared.py`, and
`server/internal/game/game_test.go` are productive but heavy. Future slices should extract helpers
when they touch these areas.

### Theme 4 - One v70 correctness finding remains worth doing

Realtime fanout still reads player current levels after unlocking the session loop. It is bounded
and should be a high-value maintenance slice.

## 4. Top 10 recommendations

1. Snapshot realtime client levels under lock before `fanoutResult`.
2. Extract combat event presentation from `client/scripts/main.gd`.
3. Guard `env["payload"]` access in `client/scripts/main.gd`.
4. Derive client bot `ALL_STEP_TYPES` from wait/assert/action lists.
5. Split future dungeon-generation tests out of `game_test.go`.
6. Keep pack leader data internal until a UI/gameplay feature needs protocol exposure.
7. Split `tools/bot/run.py` into driver, assertions, and special co-op modules.
8. Split `tools/validate_shared.py` by validation domain.
9. Prefer semantic generated-content assertions over exact population counts.
10. Feed v80 review findings into `/next` before starting the next feature batch.

## 5. What done well looks like here

- Gameplay facts stay server-owned and replayable.
- Shared rules and schemas validate content before runtime.
- Client presentation is proved through headless client bots, not manual inspection alone.
- Specs, plans, as-built notes, and review findings remain connected to committed slices.
- Maintenance work is small and CI-backed, not broad cleanup.

*Evidence: direct reads of current repo docs/code, v70 review set, `PROGRESS.md`, `CLAUDE.md`,
`AGENTS.md`, shared rules, Go server, Godot client, Python bots/validator, and focused v80
verification. Commands run for v80: `make client-unit`,
`SCENARIO=31_combat_threat_readability HEADLESS=1 ./scripts/bot_client_local.sh`, and
`ARPG_ADDR=:8888 SCENARIO=pack_aggro_and_dungeon_packs ./scripts/bot_local.sh`, and `make ci`;
all passed.*
