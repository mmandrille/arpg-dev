# arpg-dev - Engineering review at slice **v200** (overview)

**Date:** 2026-06-15
**Reviewer:** Codex
**Scope:** Whole repo - Go authoritative server, Godot thin client, shared contracts, Python tooling, progress docs, and SDD/review process at the repaired v200 gate.
**Git baseline:** `main` at `310b67d4` (`docs: add v200 engineering review`). Worktree was clean before the requested deletion/regeneration of the previous v200 review files; during this review run the only pre-existing dirt was those deleted review files.
**Companion reports:**
- [`backend/20260615_v200-backend.md`](backend/20260615_v200-backend.md) - Go authoritative server
- [`client/20260615_v200-client.md`](client/20260615_v200-client.md) - Godot thin client
- [`extras/20260615_v200-shared-tooling-and-process.md`](extras/20260615_v200-shared-tooling-and-process.md) - contracts, Python tooling, docs/SDD

---

## 1. Executive summary

The v200 baseline is green after the review-gate repair commits. `make ci` passed in 7m44s, including file-size ratchet, extraction-coupling ratchet, shared validation, asset validation, determinism lint, Go tests, Python tests, protocol bot + replay, 45/45 client bot scenarios, and Godot headless smoke.

The earlier shared skill prerequisite regression is resolved. Rage no longer depends on Ranger Piercing Shot, Volley again follows Piercing Shot, and Split Arrow is the tier-3 Volley follow-up. The repaired rows are covered by Go tests, client loader tests, protocol bot scenarios, client bot scenarios, and `tools/validate_skills.py`.

The repo is safe to resume feature planning, with caveats. Maintainability is green by ratchet but still carries large-file debt after a baseline refresh. Progress docs are smaller, but moved archive links likely need repair. Companion/elite-minion tuning and monster-rarity expected values remain the most concrete shared-data ownership follow-ups.

## 2. Scorecard

| Area | Score | Direction | Evidence |
|------|-------|-----------|----------|
| Architecture (boundaries) | 8.8 | Improved | Server authority remains intact; repaired skill data flows through Go, protocol bot, and client without client shortcuts. |
| Architecture (internal cohesion) | 8.2 | Stable | Focused helpers exist, but `main.gd`, `sim.go`, `game_test.go`, `rules.go`, and `validate_shared.py` remain large pressure points. |
| Technical correctness | 9.0 | Improved | `make ci` passed in 7m44s; previously failing Rage/Ranger/client skill scenarios now pass. |
| Test quality | 9.0 | Improved | Multi-layer tests caught the prior regression, and `tools/validate_skills.py` now guards the prerequisite chain. |
| Maintainability | 8.0 | Mixed | Ratchets pass, but several baselines were refreshed upward rather than reduced. |
| Documentation | 8.4 | Mixed | `PROGRESS.md` compaction is useful; progress archive links still need repair. |
| Process / SDD | 8.7 | Improved | Review cadence ran with `$refactor` first and CI-backed review evidence; the deleted v200 review set was regenerated on request. |
| Overall | 8.6 | Improved | Green enough for `$next`, while maintainability and progress-link hygiene should steer early follow-ups. |

## 3. Cross-cutting themes

1. **The repair path validated the architecture.** One shared rule fix repaired Go, protocol, and client behavior together.
2. **CI has high signal.** It caught the previous regression and now proves the repaired baseline across 11 steps.
3. **Maintainability is green but not paid down.** Baseline refreshes restored the gate, but large files remain large.
4. **Progress docs are more usable at task start.** `PROGRESS.md` is a concise dashboard, with history split into `docs/progress/`.
5. **Link hygiene is the next process cleanup.** Moved archive files still contain many root-style `docs/...` links.

## 4. Top 10 recommendations

1. Fix or validate relative links in `docs/progress/slice-lifecycle.md` and `docs/progress/shipped-changelog-archive.md`.
2. Move companion and elite-minion follow/assist tuning into shared rules while preserving current values.
3. Convert `validateMonsterRarities` hardcoded expected values into a named golden/schema-owned validation path.
4. Split the next touched status/debug/presentation path out of `client/scripts/main.gd`.
5. Extract the next touched class/skill assertion cluster from `server/internal/game/game_test.go`.
6. Continue shrinking `tools/validate_shared.py` by extracting item rarity, monster rarity, or unique-effect validators.
7. Keep new companion UI work in `companion_bar.gd` or focused helpers, not `main.gd`.
8. Add a direct make target or documentation for `scripts/check-progress-dashboard.sh`.
9. Treat file-size baseline refreshes as temporary process repairs; future slices touching those files should shrink them.
10. Preserve `tools/validate_skills.py` prerequisite checks for any future class tree changes.

## 5. What "done well" looks like here

- `make ci` stays green before `$next` proposes a new feature batch.
- Shared skill and rules changes have semantic validation, not just JSON schema validation.
- Client presentation continues to derive display facts from shared rules or protocol state.
- Large-file edits include local extraction or a clear maintenance payback.
- Progress docs stay slim and all archive links resolve from their actual locations.

## Verification

- `make ci` - passed in 7m44s on 2026-06-15.
  - Passed: file-size ratchet, extraction-coupling ratchet, shared schema validation, asset validation, determinism lint, Go tests, Python tests, server build/start, protocol bot + replay, 45/45 Godot client bot scenarios, and Godot headless smoke.
- The previously failing scenarios now pass inside CI:
  - protocol: `rage_and_heal_skills`, `barbarian_class_foundation`, `ranger_volley_and_visual_showcase`, `mana_regeneration`;
  - client: `client_skill_points_and_magic_bolt`.

*Evidence: companion reports above; current stats from `find`, `wc -l`, and `rg`; baseline from `git rev-parse --short HEAD`, `git log -1 --oneline`, and `git status --short`.*
