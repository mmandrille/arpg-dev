# arpg-dev — Engineering review at slice **v334** (overview)

**Date:** 2026-06-25
**Reviewer:** Claude (Cursor agent), sequential backend/client/extras audits.
**Scope:** Whole repo — delta since v331 review (v332–v334 stabilization + feature slices). Ad hoc review: user-requested; official cadence target remains v340.
**Git baseline:** `main` at `9d02179b` (`fix: restore bot walk routing for open lab worlds and vendor hub bounds`). Worktree dirty: wall-floor shader WIP (`main.gd` +116, bot handlers, scenario JSON, untracked `wall_floor_lab_nav_test.go`).
**Companion reports:**
- [`backend/20260625_v334-backend.md`](backend/20260625_v334-backend.md) — Go authoritative server
- [`client/20260625_v334-client.md`](client/20260625_v334-client.md) — Godot thin client
- [`extras/20260625_v334-shared-tooling-and-process.md`](extras/20260625_v334-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

The v331 review ended with a **red test suite** and stale CODEMAP — both are largely resolved. v332 navigation and golden repairs restored **`go test ./...` green**; v333 delivered room-layout spawn awareness; v334 added data-driven ranged-retreat engagement gating. Post-v331 `$refactor` paydown also landed: CODEMAP inverse check, fog schema `required` fields, fog semantic range guards, ADR-0015, fog overlay baseline, and CLAUDE.md spec-gate exemption.

The period's primary risk shifted from **technical correctness** to **maintainability**: `main.gd` now **fails the file-size ratchet** (6,239 vs 6,132+25 cap), and uncommitted wall-floor WIP adds another ~116 lines. `sim.go` grew to 6,632 without net extraction. Architecture boundaries and data-driven discipline remain strong.

## 2. Scorecard

| Area | Score | Direction vs v331 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority on movement, spawn, ranged retreat; client WIP is presentation-only. |
| Architecture (internal cohesion) | 7.5 | → stable | `sim.go`/`rules.go`/`main.gd` still oversized; focused files appear alongside coordinators. |
| Technical correctness | 8.8 | ↑↑ from 6.5 | `go test ./...` green; determinism lint + `go vet` clean; `validate-shared` OK. |
| Test quality | 8.0 | ↑ from 7.5 | v331 failures resolved; v333/v334 focused Go tests; wall-floor nav test WIP uncommitted. |
| Maintainability | 7.0 | ↓ from 7.5 | **`main.gd` ratchet FAIL**; `sim.go` +91 lines; `rules.go` unchanged at 3,312. |
| Documentation | 8.5 | ↑ from 7.0 | CODEMAP + inverse check; ADR-0015; spec exemption; v332–v334 as-builts. |
| Process / SDD | 8.5 | ↑ from 8.2 | v332–v334 as-builts; presentation exemption documented; ad hoc review noted. |
| **Overall** | **8.2** | **↑ from 7.5** | Test suite and docs paydown recovered confidence; `main.gd` ratchet is the new gate. |

## 3. Cross-cutting themes

1. **v331 blocker cleared.** Test failures were real and are fixed — navigation cell-center blocking, planPath goal allowance, replay seed, ranged clear-shot validation. Do not regress this with rushed `main.gd` growth.

2. **`main.gd` is the new CI gate.** `make maintainability` fails until ≥82 lines net-extract from `main.gd` or WIP is reduced. Wall-floor shader work must land with extraction or as a documented baseline exception — not silent growth.

3. **v331 `$refactor` queue largely complete.** CODEMAP, fog schema/guards, ADR-0015, fog baseline, spec exemption — done. Open: `rules.go` type extraction, `main.gd` movement presenter, `sim.go` tick context (future).

4. **Coordinator debt compounds silently.** `sim.go` at 6,632 absorbs tick logic; extraction program needs a typed context starting point.

5. **Full `make ci` still the integration proof.** Spot checks green; PROGRESS.md notes batch CI pending. Run before next feature slice ships.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature slice; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Maint]** Fix `main.gd` ratchet breach — extract movement-intent or wall-floor presentation cluster; net-remove lines until ≤6,157. Do not merge wall-floor WIP without this.

2. **[minor · Maint]** Extract `DungeonGenerationRules` type cluster from `rules.go` to `dungeon_generation_rules.go` (~200 lines).

3. **[minor · Test]** Commit `wall_floor_lab_nav_test.go` with scenario 78 when WIP stabilizes.

4. **[minor · Test]** Add `test_validate_main_config.py` for validator-module consistency.

5. **[minor · Schema]** `cross_checks()` link between `camera_presentations` modes and fog `organic_edge` keys.

6. **[minor · Maint]** Route minimap facing sync through a narrow context object instead of `main.gd` tentacle.

7. **[future]** Typed tick-context struct for `sim.go` incremental extraction.

8. **[future]** Full `sim.go` coordinator paydown — multi-slice.

9. **[future]** `validate_shared.py` validation-domain extraction.

10. **[process]** Run full `make ci` and update PROGRESS.md CI gate line when green.

## 5. What "done well" looks like here

- `make maintainability` green — `main.gd` at or below ratchet cap with net extraction, not baseline inflation.
- `go test ./...` and `make ci` green on `main` before the next feature slice.
- CODEMAP inverse check keeps passing as new files land.
- Every new tuning knob in `main_config.v0.json` has rules validation + semantic guard where client-visible.
- Coordinator baselines trend down quarter-over-quarter.

## Verification

- `cd server && go test ./...` — **PASSED** (~246s game package, 2026-06-25).
- `cd server && go vet ./...` — passed.
- `make validate-shared` — passed (1,555 checks).
- `tools/validate_codemap.py` — passed (inverse check included).
- `make lint-determinism` — passed.
- `make maintainability` — **FAILED** (`main.gd` exceeds baseline + 25).
- `.venv/bin/pytest tools/bot/test_protocol.py` — 64 passed.
- `make client-unit` — not re-run (godot path unavailable in review shell).
- `make ci` (full gate) — **not run**.

*Evidence: companion reports; command output 2026-06-25; git log v332–v334; PROGRESS.md v331 follow-up status.*
