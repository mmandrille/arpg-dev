# arpg-dev — Engineering review at slice **v337** (overview)

**Date:** 2026-06-25
**Reviewer:** Claude (Cursor agent), sequential backend/client/extras audits.
**Scope:** Whole repo — delta since v334 review (v335–v337 paydown + post-v334 `$refactor` completion). User-requested ad hoc review at v337; official cadence target remains v340 with green `make ci`.
**Git baseline:** `main` at `da9a47b4` (`test: add unit tests for fog presentation validators`). Worktree clean.
**Companion reports:**
- [`backend/20260625_v337-backend.md`](backend/20260625_v337-backend.md) — Go authoritative server
- [`client/20260625_v337-client.md`](client/20260625_v337-client.md) — Godot thin client
- [`extras/20260625_v337-shared-tooling-and-process.md`](extras/20260625_v337-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

The v334 review ended with a **maintainability ratchet failure on `main.gd`** and an open v334 `$refactor` queue. Both are largely resolved. v335 extracted `movement_input_presenter.gd`; v336 landed wall-floor lab nav proof; v337 introduced `simTickCtx` as the first sim coordinator paydown seam; post-v334 `$refactor` fixed `test_item_visuals.gd` ratchet and added fog validator unit tests. **`make maintainability` now passes**; `main.gd` has only 9 lines of headroom under its cap.

Technical correctness remains strong: `go vet ./...` clean, `make validate-shared` 1,557 checks OK, `make lint-determinism` OK. The period's primary risks shifted from **ratchet breach** to **coordinator fragility** (`main.gd` headroom, `sim.go` at 6,632 lines without net shrink) and **integration CI** (batch `make ci` still pending on host). A new backend finding: `realtime/runner.go` (~649 lines) has zero call sites.

## 2. Scorecard

| Area | Score | Direction vs v334 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority on movement, spawn, ranged retreat, tick context; client WIP resolved; thin-client preserved. |
| Architecture (internal cohesion) | 7.5 | → stable | `sim.go`/`rules.go`/`main.gd` still oversized; focused files (`sim_tick_context.go`, `movement_input_presenter.gd`, item-visual probes) appear alongside coordinators. |
| Technical correctness | 8.8 | → stable | `go vet` clean; determinism lint OK; `validate-shared` OK; full `go test ./...` not re-run (~4 min). |
| Test quality | 8.3 | ↑ from 8.0 | Fog/main_config validator tests; item-visual probe extraction; v336 nav proof committed. |
| Maintainability | 8.0 | ↑↑ from 7.0 | **`make maintainability` PASS**; `test_item_visuals.gd` dropped from baseline; `main.gd` passes with 9-line headroom. |
| Documentation | 8.0 | ↓ from 8.5 | v335/v337 refactor slices lack specs/plans; v337 as-built thin; this review closes the gap. |
| Process / SDD | 8.2 | ↓ from 8.5 | Refactor slices documented in as-built only; batch `make ci` still pending. |
| **Overall** | **8.4** | **↑ from 8.2** | Maintainability recovery and validator test coverage; coordinator debt and CI integration remain. |

## 3. Cross-cutting themes

1. **v334 `$refactor` queue largely complete.** CODEMAP, fog schema/guards, ADR-0015, movement presenter, wall-floor nav test, sim tick context starter, item-visual probes, fog validator tests — done. Open: `sim.go` phase extraction, `rules.go` validator split, `validate_shared.py` domain extraction.

2. **`main.gd` ratchet resolved, fragility remains.** 6,143 lines vs 6,152 cap — passes today, fails on any +10-line slice without extraction. Attack-move cluster is the highest-value next seam.

3. **v337 tick context is foundation, not paydown.** `sim.go` grew +5 vs baseline; `simTickCtx` enables incremental phase-helper extraction without determinism risk.

4. **Dead `runner.go` is new drift risk.** 649 lines, zero call sites, divergent tick API from production `session_loop`. Quarantine before it silently re-enters the path.

5. **Full `make ci` still the integration proof.** Spot checks green; PROGRESS.md notes batch CI pending. Stabilize before v340 milestone review.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature slice; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Maint]** Extract attack-move / sticky-target cluster from `main.gd` — only 9 lines under ratchet cap; largest remaining input seam.

2. **[minor · Maint]** Remove or quarantine `realtime/runner.go` (~649 lines, zero call sites).

3. **[future · Maint]** Continue `sim.go` paydown via `simTickCtx` phase helpers — multi-slice; v337 starter landed.

4. **[minor · Maint]** Extract dungeon-generation validators from `rules.go` into companion file (~200 lines).

5. **[minor · Test]** Deepen `test_movement_input_presenter.gd` — mock client for keyboard/stop intent paths.

6. **[minor · Arch]** Harden `equipped_update` delta path — `c.get("slot", "")` + malformed-delta test.

7. **[minor · Schema]** Add `point_light` to fog schema top-level `required`.

8. **[future]** `validate_shared.py` validation-domain extraction.

9. **[blocker · Process]** Stabilize batch `make ci` and update PROGRESS.md CI gate line.

10. **[minor · Maint]** Extract loot-label presentation from `main.gd` into factory/filter module.

## 5. What "done well" looks like here

- `make maintainability` green with **headroom** on `main.gd` (not just passing at +9 lines).
- `go test ./...` and `make ci` green on `main` before the v340 milestone review.
- `sim.go` trending down quarter-over-quarter via `simTickCtx` phase extractions.
- Every extracted validator module has a matching `test_*.py`.
- CODEMAP inverse check keeps passing as new files land.
- Refactor slices either have lightweight specs or explicit `$refactor` exemption notes in as-built.

## Verification

- `make maintainability` — **PASSED** (2026-06-25).
- `make validate-shared` — **PASSED** (1,557 checks + codemap).
- `cd server && go vet ./...` — **PASSED**.
- `make lint-determinism` — **PASSED** (via maintainability gate).
- `.venv/bin/pytest tools/test_validate_fog_presentation.py tools/test_validate_main_config.py tools/test_validate_codemap.py tools/bot/test_protocol.py -q` — **72 passed**.
- `godot --headless --path client --script res://tests/test_item_visuals.gd` — **PASSED** (spot check).
- `cd server && go test ./...` — **not re-run** (~4 min; v334 evidence green).
- `make client-unit` — **not re-run**.
- `make ci` (full gate) — **not run** (PROGRESS.md: pending on host).

*Evidence: companion reports; command output 2026-06-25; git log v335–v337; PROGRESS.md; v334 review comparison.*
