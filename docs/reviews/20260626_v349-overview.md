# arpg-dev — Engineering review at slice **v349** (overview)

**Date:** 2026-06-26  
**Reviewer:** Claude (Cursor agent), sequential backend/client/extras audits + `make ci-full` gate.  
**Scope:** Whole repo — v340–v349 presentation/combat-pressure batch, v348 renderer migration, v349 tick smoothing, CI/smoke stabilization, orphan asset cleanup. Official cadence review (12 slices past v337 ad hoc; v340 milestone passed).  
**Git baseline:** `main` @ `82422216` (`chore: remove unmanifested client assets and gate orphans in CI`). Worktree: uncommitted `skills/review/SKILL.md` (ci-full gate wording).  
**Verification:** `make ci-full` — **FAILED** in **37m05s** (steps 9–10). `make ci` — **green** (user-confirmed; pack gate). `make validate-shared` — **1,564 checks OK**. `make maintainability` — **PASS** (34 grandfathered files, 64,429 lines).  
**Companion reports:**
- [`backend/20260626_v349-backend.md`](backend/20260626_v349-backend.md) — Go authoritative server
- [`client/20260626_v349-client.md`](client/20260626_v349-client.md) — Godot thin client
- [`extras/20260626_v349-shared-tooling-and-process.md`](extras/20260626_v349-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

v337 closed the **maintainability ratchet breach** and left coordinator paydown + **full CI integration** as the main risks. v340–v349 shipped a dense presentation/combat batch (depth pressure, loot beams, ambient polish, vignette/cooldown cues, windup/telegraph, render perf, forward_plus, entity tick smoothing) while keeping **protocol v8** stable.

**Merge-gate health is good:** `make ci` green, headless smoke equip stabilized, orphan asset gate added. **Full-matrix health is not:** `make ci-full` failed with **12 extended protocol** and **3 extended client-bot** scenarios — unique-effect suites (including a **762s** run), coop/gold flows, dungeon elite, and three client UI/dungeon proofs. The gap between fast pack CI and extended matrix is now the primary release-confidence risk.

Coordinator debt is familiar: `sim.go` (+14 vs baseline, within ratchet), dead `runner.go` (649 lines), `main.gd` (6,192 lines, ~20 lines headroom), `validate_shared.py` monolith. v349 added **good focused extractions** (tick smoothing runtime, Godot CI flags, asset orphan check) without shrinking the largest coordinators.

## 2. Scorecard

| Area | Score | Direction vs v337 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority; thin client; presentation-only v348–v349; no protocol drift. |
| Architecture (internal cohesion) | 7.5 | → stable | `sim.go` / `main.gd` / `validate_shared.py` still dominate; focused modules appear alongside. |
| Technical correctness | 8.5 | ↓ from 8.8 | Pack CI + unit gates green; **ci-full extended failures** (15 scenarios). |
| Test quality | 8.0 | ↓ from 8.3 | 8 orphan client unit tests; extended bot matrix exposes flakes/timeouts; smoke equip fixed. |
| Maintainability | 8.2 | ↑ from 8.0 | Ratchet green; orphan asset gate; `main.gd` headroom improved vs v337 (+20 vs +9). |
| Documentation | 7.8 | ↓ from 8.0 | v348–v349 SDD complete; v340–v347 plan gaps; PROGRESS/review-gate drift. |
| Process / SDD | 7.5 | ↓ from 8.2 | ci-full vs ci gate mismatch; extended scenario debt; lifecycle table stale rows. |
| **Overall** | **8.1** | ↓ from 8.4 | Strong merge gate + boundaries; full-matrix CI and coordinator debt pull score down. |

## 3. Cross-cutting themes

1. **Two-speed CI.** Fast pack (`make ci`, 36 scenarios, ~6 min) is green. Full matrix (`make ci-full`, ~196 scenarios, ~37 min) failed — extended tier is not merge-gated but is the right bar for periodic reviews.

2. **v337 refactor queue mostly open on coordinators.** `simTickCtx` paydown, `runner.go` quarantine, `rules.go` validator split, `main.gd` attack-move extraction, orphan smoke tests — all still pending. v345-style micro-extractions (`monster_melee_windup.go`) prove the pattern works.

3. **Presentation batch landed without wire changes.** v348 renderer + v349 tick smoothing are data-driven and bot-proven (extended). Good template for future client-only slices.

4. **Smoke equip hardening closed a real flake.** `weapon_sets` sync in `smoke.gd` fixed headless CI failures unrelated to v348/v349 features.

5. **Asset hygiene ratchet.** Orphan `client/assets` gate prevents repeat of accidental Godot import commits.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature batch; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Test]** Triage **`make ci-full` failures** — 12 protocol + 3 client extended scenarios; start with `resource_support_mobility_unique_effects` (762s budget) and `wall_floor_dungeon_rollout` (level/wall layout).

2. **[blocker · Maint]** Extract **attack-move / sticky-target** cluster from `main.gd` (~20 lines ratchet headroom).

3. **[minor · Maint]** **Quarantine `realtime/runner.go`** (649 lines, zero call sites).

4. **[minor · Test]** Register **8 orphan client unit tests** in `client_smoke.sh` (market/bishop/material domains).

5. **[future · Maint]** Continue **`sim.go` paydown** via `simTickCtx` phase files.

6. **[minor · Process]** Sync **`PROGRESS.md`** with review skill — periodic gate = `make ci-full`; clear stale “batch ci pending” follow-up.

7. **[minor · SDD]** Backfill **plans or exemption notes** for v340–v347; update **scenario catalog** for v348–v349.

8. **[minor · Schema]** Add **`point_light`** to fog schema top-level `required` (v337 carry-over).

9. **[future · Maint]** **`validate_shared.py` domain extraction** + boss/item presentation validator tests.

10. **[minor · Arch]** Resolve **local-player tick smoothing** spec vs scenario 84 implementation drift.

## 5. What "done well" looks like here

- `make ci-full` green on `main` before the v359 milestone review.
- Extended scenario failures either fixed or explicitly owned with budgets/timeouts — no 12+ scenario failure backlog.
- `main.gd` and `sim.go` trending down quarter-over-quarter via focused extractions.
- Every new `client/tests/test_*.gd` registered in `client_smoke.sh` the same slice it lands.
- Periodic reviews record **ci-full** outcome, not just pack CI.
- Presentation-only slices either have lightweight plans or documented spec-gate exemptions in lifecycle.

## Verification (detail)

| Check | Result |
|-------|--------|
| `make maintainability` | **PASS** (2026-06-26) |
| `make validate-shared` | **PASS** (1,564 checks + codemap) |
| `make ci` | **PASS** (merge pack) |
| `make ci-full` | **FAIL** — 37m05s; steps 9–10 failed |
| Headless smoke (step 11) | **PASS** |

**Full failure inventory:** [`20260626_v349-ci-full-failures.md`](20260626_v349-ci-full-failures.md) — all 15 scenarios, JSON paths, failing steps, and focused rerun commands (no need to re-run full CI during `$refactor`).

### Protocol extended failures (12)

| Scenario ID | File | Time |
|-------------|------|------|
| `full_equipment` | `tools/bot/scenarios/19_full_equipment.json` | 4.96s |
| `monster_rarity_loot_scaling` | `tools/bot/scenarios/21_monster_rarity_loot_scaling.json` | 29.53s |
| `inventory_capacity_and_paper_doll` | `tools/bot/scenarios/25_inventory_capacity_and_paper_doll.json` | 4.17s |
| `coop_rewards_and_scaling` | `tools/bot/scenarios/34_coop_rewards_and_scaling.json` | 31.87s |
| `gold_autopickup_shared_loot` | `tools/bot/scenarios/35_gold_autopickup_shared_loot.json` | 20.29s |
| `pack_aggro_and_dungeon_packs` | `tools/bot/scenarios/42_pack_aggro_and_dungeon_packs.json` | 32.32s |
| `offensive_unique_effects` | `tools/bot/scenarios/54_offensive_unique_effects.json` | 7.36s |
| `survival_reactive_unique_effects` | `tools/bot/scenarios/55_survival_reactive_unique_effects.json` | 27.18s |
| `resource_support_mobility_unique_effects` | `tools/bot/scenarios/56_resource_support_mobility_unique_effects.json` | **762.62s** |
| `dungeon_elite_side_objective` | `tools/bot/scenarios/68_dungeon_elite_side_objective.json` | 66.65s |
| `live_rare_combat_affixes` | `tools/bot/scenarios/79_live_rare_combat_affixes.json` | 4.16s |
| `mercenary_foundation` | `tools/bot/scenarios/86_mercenary_foundation.json` | 11.47s |

### Client extended failures (3)

| Scenario ID | File | Failing step | Symptom |
|-------------|------|--------------|---------|
| `character_stats_panel` | `tools/bot/scenarios/client/09_character_stats_panel.json` | step 4 `wait_event` (1.0s) | No `monster_killed` after combat clicks |
| `blacksmith_armor_recipe` | `tools/bot/scenarios/client/70_blacksmith_armor_recipe.json` | step 35 `wait_blacksmith_panel` (15s) | Blacksmith panel state never matched (`cave_mail`, upgrade enabled) |
| `wall_floor_dungeon_rollout` | `tools/bot/scenarios/client/79_wall_floor_dungeon_rollout.json` | step 2 `wait_wall_layout` (15s) | Stayed on level 0; 0 generated walls (expected level -1) |

*Evidence: `/tmp/arpg-ci-full-review.log`; quiet mode — use `VERBOSE=1` on focused reruns for protocol assertion text.*
