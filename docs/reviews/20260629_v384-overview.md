# arpg-dev — Engineering review at slice **v384** (overview)

**Date:** 2026-06-29  
**Reviewer:** Cursor agent — sequential backend/client/extras audits + `make ci-full` gate.  
**Scope:** Whole repo — v370–v384 combat/exploration perf batch (15 slices), v350 ci-full recovery cohort, post-batch coordinator and CI posture. Official cadence review (v349 was 2026-06-26; ~v359 milestone passed).  
**Git baseline:** `main` @ `625f5600` (`fix: v384: rename MainConfigLoader presentation_lod accessor`). Worktree: **clean**.  
**Verification:** `make ci-full` — **FAILED** in **22m03s** (steps 9–10). `make ci` — **green** (merge pack, 2026-06-29 post-v384). `make validate-shared` — pass (spot-check). `make maintainability` — **PASS** (34 grandfathered files, 64,522 lines).  
**Companion reports:**
- [`backend/20260629_v384-backend.md`](backend/20260629_v384-backend.md) — Go authoritative server
- [`client/20260629_v384-client.md`](client/20260629_v384-client.md) — Godot thin client
- [`extras/20260629_v384-shared-tooling-and-process.md`](extras/20260629_v384-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

v370–v384 shipped a dense **combat/exploration performance batch**: client-side presentation degradations (fog throttle, entity LOD, VFX/projectile caps, delta coalescing, recon backpressure, perf phase breakdown) and server-side load shedding (collision tick cache, combat tick budget, overload degradation, persist defer). Protocol v8 and authoritative boundaries stayed stable; tuning moved into shared rules where appropriate.

**Merge-gate health is good:** `make ci` green. **Full-matrix health regressed:** `make ci-full` failed with **1 protocol + 2 client extended** scenarios — a *different* set than v349’s 15 failures (that cohort passed in this run, consistent with v350 recovery). New failures: `companion_rank_scaling_and_limits`, `client_skill_points_and_magic_bolt` (event consumption timing), `interactable_tick_smoothing` (no interactable spawned).

Coordinator debt is familiar but shifted: `sim.go` shrank ~78 lines since v349; `main.gd` grew +174 lines despite extractions; dead `runner.go` (~650 lines) diverges further from production. v370–v384 SDD as-builts are mostly stubs — traceability gap for the largest batch since v340–v349.

## 2. Scorecard

| Area | Score | Direction vs v349 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority; thin client; perf degrades presentation only; protocol v8 unchanged. |
| Architecture (internal cohesion) | 7.3 | ↓ from 7.5 | `main.gd` +174 lines; attack-move cluster unextracted; `sim.go` slight shrink; good focused modules alongside coordinators. |
| Technical correctness | 8.3 | ↓ from 8.5 | Pack CI green; **ci-full 3 extended failures** (new set, not v349 cohort). |
| Test quality | 7.8 | ↓ from 8.0 | 8 orphan client tests; perf logic mostly untested at unit level; crowded probe in pack. |
| Maintainability | 8.0 | ↓ from 8.2 | Ratchet green; `main.gd` at ceiling; `runner.go` drift; `sim.go` trending down. |
| Documentation | 7.5 | ↓ from 7.8 | v370 good; v372–v378 stub as-builts; PROGRESS ci-full narrative stale. |
| Process / SDD | 7.3 | ↓ from 7.5 | 9-slice batch with thin SDD; ci-full gate recorded; extended debt rotated not cleared. |
| **Overall** | **8.0** | ↓ from 8.1 | Strong boundaries + merge gate; full-matrix failures and coordinator/test debt pull score down. |

## 3. Cross-cutting themes

1. **Two-speed CI persists.** Fast pack (`make ci`, ~6 min) green. Full matrix (`make ci-full`, ~22 min) failed — **3 extended scenarios**. v349’s 15-failure backlog recovered; new failures need triage before v394 review.

2. **Perf batch landed without wire changes.** v370–v384 is a template for data-driven presentation/server load-shedding — but SDD traceability did not keep pace (stub as-builts).

3. **v337 refactor queue mostly open.** `runner.go` quarantine, `simTickCtx` paydown, `main.gd` attack-move extraction, orphan smoke tests, `validate_shared.py` split — still pending. v381–v384 extractions prove the pattern (`tick_collision_cache.go`, `persist_defer.go`).

4. **`sim.go` trending down; `main.gd` trending up.** Net coordinator movement is wrong direction on client despite perf module extractions.

5. **Dead `runner.go` is escalating risk.** Production gained defer + guardrails + profiled ticks; dead path did not — reactivation would silently regress perf behavior.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature batch; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Test]** Triage **`make ci-full` failures** — `companion_rank_scaling_and_limits`, `client_skill_points_and_magic_bolt`, `interactable_tick_smoothing`.

2. **[blocker · Maint]** Extract **attack-move / sticky-target** cluster from `main.gd` (~20 lines ratchet headroom).

3. **[minor · Maint]** **Quarantine `realtime/runner.go`** (~650 lines, zero call sites; annotate CODEMAP as dead).

4. **[minor · Test]** Register **8 orphan client unit tests** in `client_smoke.sh` (market/bishop/material domains).

5. **[future · Maint]** Continue **`sim.go` paydown** via `simTickCtx` phase files.

6. **[minor · Maint]** Deduplicate **`advanceMonsterMovementBudgeted`** vs `advanceMonsterMovement` (`combat_tick_budget.go`).

7. **[minor · SDD]** Backfill **v372–v378 as-builts** with behavior + verification (v370 template).

8. **[minor · Process]** Sync **`PROGRESS.md`** ci-full status — v350 recovery vs current 3 failures.

9. **[minor · Schema]** Add **`point_light`** to fog schema top-level `required` (v337 carry-over).

10. **[minor · Test]** Add focused unit tests for **LOD, projectile cap, delta coalesce, recon backpressure**.

## 5. What "done well" looks like here

- `make ci-full` green on `main` before the v394 milestone review.
- Extended scenario failures fixed or explicitly owned — no rotating 3+ scenario failure backlog.
- `main.gd` and `sim.go` trending down quarter-over-quarter via focused extractions.
- Every perf slice has a substantive as-built with verification commands.
- Dead `runner.go` removed or build-tagged quarantine — no silent divergence from production.
- New `client/tests/test_*.gd` registered in `client_smoke.sh` the same slice it lands.

## Verification (detail)

| Check | Result |
|-------|--------|
| `make maintainability` | **PASS** (2026-06-29; 34 files, 64,522 lines) |
| `make validate-shared` | **PASS** (spot-check) |
| `make ci` | **PASS** (merge pack, post-v384) |
| `make ci-full` | **FAIL** — 22m03s; steps 9–10 failed |
| Headless smoke (step 11) | **PASS** (74 GDScript unit gates) |

### Extended failures (3)

| Scenario ID | Tier | File | Symptom |
|-------------|------|------|---------|
| `companion_rank_scaling_and_limits` | protocol | `tools/bot/scenarios/*companion_rank*` | Failed in 15.73s (assertion detail — rerun with `VERBOSE=1`) |
| `client_skill_points_and_magic_bolt` | client | `tools/bot/scenarios/client/*skill_points*` | Step 23 `wait_event` `skill_cooldown_started` timeout; event listed in `pending_events` |
| `interactable_tick_smoothing` | client | `tools/bot/scenarios/client/*interactable_tick*` | Step 1 `wait_entity` interactable — `interactable_count=0` for 10s |

*Evidence: `/tmp/arpg-ci-full-review.log`; focused reruns: `VERBOSE=1 make bot scenario=companion_rank_scaling_and_limits`, `VERBOSE=1 make bot-client SCENARIO=client_skill_points_and_magic_bolt HEADLESS=1`.*
