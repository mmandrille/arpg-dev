# arpg-dev — Engineering review at slice **v398** (overview)

**Date:** 2026-07-01  
**Reviewer:** Cursor agent — sequential backend/client/extras audits + `make ci-full` gate.  
**Scope:** Whole repo — v385–v398 slices (economy/itemization v390–v397, codex v396, archetype library v397, client WS reconnect v398) vs v384 baseline. Official cadence review (~v394 milestone passed; last review v384 @ 2026-06-29).  
**Git baseline:** `main` @ `e0f402e4` (`fix: update set and named unique display name grammar tests`). Worktree: **clean**.  
**Verification:** `make ci-full` — **FAILED** in **33m12s** (step 10/11). `make validate-shared` — **PASS** (1,818 checks). `make maintainability` — **PASS** (35 grandfathered files, 65,746 lines).  
**Companion reports:**
- [`backend/20260701_v398-backend.md`](backend/20260701_v398-backend.md) — Go authoritative server
- [`client/20260701_v398-client.md`](client/20260701_v398-client.md) — Godot thin client
- [`extras/20260701_v398-shared-tooling-and-process.md`](extras/20260701_v398-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

v385–v398 shipped **economy/itemization depth** (upgrade shards, renew stones, blacksmith flows, tier-2 skills, item archetype library), **game codex** (v396 content compiler + client panel), and **client WebSocket reconnect** (v398 — extracted modules, data-driven backoff, no protocol change). v384 paydown themes **mostly held** on the server: `runner.go` deleted, `sim.go` stable, focused domain modules grew without wire churn; protocol remains **v8**.

**Merge-gate posture is unclear from this run** (pack-only `make ci` not re-run here). **Full-matrix health regressed sharply:** `make ci-full` **failed** at step 10 with **19 of 99 client extended scenarios** exiting code 1 — concentrated in market UI, blacksmith UI, material wallet, codex, badge reward, and hit-stop polish. Protocol bot + replay (step 9) and headless smoke (step 11) **passed**.

Coordinator debt is the recurring theme: `main.gd` grew **+188 lines** since v384 despite attack-move and reconnect extractions; panel god-files crept. v398 reconnect has **no E2E bot proof** (unit state machine only). Shared tooling debt (`validate_shared.py` monolith, duplicate scenario IDs, tuning-test audit) persists but SDD quality for v390–v398 is **much better** than the v370–v378 stub era.

## 2. Scorecard

| Area | Score | Direction vs v384 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority; thin client; v398 reconnect clears client pending state; protocol v8 unchanged. |
| Architecture (internal cohesion) | 7.4 | ↑ from 7.3 | `runner.go` gone; v398/v396 extractions good; `main.gd` +188 lines pulls down. |
| Technical correctness | 7.8 | ↓ from 8.5 post-paydown | Protocol ci-full green; **19 client extended failures**; v397 breaking DB migration documented. |
| Test quality | 7.5 | ↓ from 7.9 | 96 smoke gates; reconnect integration gap; client-bot extended matrix brittle. |
| Maintainability | 8.1 | ↑ from 8.0 | Ratchet green (35 files); `sim.go`/`game_test.go` below baselines; panel/coordinator scale. |
| Documentation | 8.0 | ↑ from 7.5 | v390–v398 as-builts substantive; v392–v394 plan gaps. |
| Process / SDD | 7.8 | ↑ from 7.3 | Better slice traceability; review cadence caught up; ci-full regression. |
| **Overall** | **7.9** | ↓ from 8.0 | Strong boundaries + server health; **client extended scenario regression** is the primary score drag. |

## 3. Cross-cutting themes

1. **Two-speed CI is back.** Protocol matrix (~11m) green including v397 `item_archetype_lab` and v390 shard scenarios. Client extended matrix (**19/99 fail**) broke the full gate — market/blacksmith/material-wallet/codex cluster. Pack (`make ci`, 14 client scenarios) may still be green; **triage extended failures before v408 review**.

2. **v398 reconnect is well-architected but under-proven.** Extraction pattern matches repo best practice; bot/replay bypass recovery; no merge-gate or extended scenario for WS drop → overlay → resync.

3. **v384 paydown held on server; client coordinator debt worsened.** Attack-move extraction closed; orphan tests 8→3; perf unit gaps closed. `main.gd` net +188 lines; `inventory_panel.gd` 1,612 lines.

4. **Economy/itemization batch (v390–v397) landed in focused modules.** `resource_loot_drops.go`, archetype templates, affix grammar, codex compiler — rules-as-data discipline intact. v397 template rename is breaking for existing DBs.

5. **Shared tooling debt is stable, not shrinking.** `validate_shared.py` ~3,033 lines; five duplicate scenario IDs (two in CI pack); tuning-friendly test audit still open.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature batch; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Test]** **Triage `make ci-full` client failures (19 extended)** — `market_purchase_ui`, `blacksmith_upgrade_ui`, `material_wallet_details`, `codex_foundation`, `badge_reward_wallet`, and 14 others. Rerun: `VERBOSE=1 make bot-client SCENARIO=all HEADLESS=1`.

2. **[blocker · Test]** Confirm **`make ci` pack** still green after failure triage — merge gate must not silently rot while extended matrix fails.

3. **[High · Maint]** Extract next **`main.gd` domain cluster** before ratchet breach — 6,554 lines vs baseline 6,548 (~19 lines headroom).

4. **[High · Maint]** Split **`inventory_panel.gd`** (1,612 lines) and parallel shop/stash/market panels.

5. **[Med · Test]** Add **`ConnectionRecoveryRuntime` integration test** + extended bot scenario (or harness hook) for v398 WS reconnect.

6. **[Med · Maint]** **Decompose `validate_shared.cross_checks()`** — ~2,800 lines in monolith (`validate_shared.py:201–3017`).

7. **[Med · Process]** **Resolve duplicate bot scenario IDs** — `mystery_seller_core`, `quest_town_turn_in` (pack members) + 3 extended pairs.

8. **[Med · Test]** **Tuning-friendly rule-test audit** — Go/GDScript/Python/bot accidental tuning pins (`PROGRESS.md` backlog).

9. **[future · Maint]** Continue **`sim.go` phase paydown** via `simTickCtx` — movement/combat/projectile helpers still in-file.

10. **[minor · SDD]** **Backfill plans for v392–v394** (specs + as-builts exist; plan column empty in lifecycle).

## 5. What "done well" looks like here

- `make ci-full` green on `main` before the v408 milestone review.
- Client extended failures fixed or explicitly demoted with budget-neutral pack curation — no 19-scenario silent backlog.
- `main.gd` and panel coordinators trending down quarter-over-quarter via focused extractions.
- v398 reconnect has integration or extended bot proof, not unit state machine only.
- `validate_shared.py` cross-checks partitioned; duplicate scenario IDs eliminated.
- Every breaking shared-ID change (v397-style) flagged in plan + `PROGRESS.md` open gaps.

## Verification (detail)

| Check | Result |
|-------|--------|
| `make maintainability` | **PASS** (2026-07-01; 35 files, 65,746 lines) |
| `make validate-shared` | **PASS** (1,818 checks) |
| `make ci-full` step 9 — protocol bot + replay | **PASS** (~11m02s) |
| `make ci-full` step 10 — client bot `SCENARIO=all` | **FAIL** — 19 of 99 scenarios |
| `make ci-full` step 11 — headless smoke | **PASS** (~1m56s) |
| **Total `make ci-full`** | **FAIL** — 33m12s |

### Client extended failures (19)

`market_purchase_ui`, `market_active_offer_ui`, `blacksmith_upgrade_ui`, `codex_foundation`, `market_cancel_listing_ui`, `my_market_offers_panel`, `buyer_offer_cancel_ui`, `market_trade_receipts`, `material_wallet_details`, `blacksmith_recipe_selector`, `market_expiration_countdown`, `market_search_sort`, `market_item_comparison`, `material_wallet_window`, `blacksmith_upgrade_history`, `market_notification_badge`, `badge_reward_wallet`, `hit_stop_impact_flash`, `blacksmith_shard_merge`

*Evidence: `/tmp/arpg-ci-full-v398-review.log`; log excerpt in CI step 10 footer.*

---

## v384 follow-up status

| v384 theme | v398 status |
|------------|-------------|
| ci-full green | **Regressed** — 19 client extended failures (protocol still green) |
| `runner.go` quarantine | **Closed** — deleted |
| `main.gd` attack-move extraction | **Closed** — `attack_move_input_coordinator.gd` |
| Orphan client tests | **Mostly closed** — 8→3 (visual probes only) |
| `sim.go` paydown | **Partial** — stable size; phase helpers still in-file |
| `validate_shared` split | **Open** — monolith persists |
| v372–v378 as-built backfill | **Superseded** — v390–v398 as-builts healthy |

*Evidence: companion reports; `make ci-full` run 2026-07-01; comparison to [`20260629_v384-overview.md`](20260629_v384-overview.md).*
