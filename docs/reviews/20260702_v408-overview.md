# arpg-dev — Engineering review at slice **v408** (overview)

**Date:** 2026-07-02  
**Reviewer:** Cursor agent — sequential backend/client/extras audits + `make ci-full` gate (in progress at review write; step 1 failed pre-refactor).  
**Scope:** Whole repo — v399–v408 autoloop batch (reconnect proof, upgrade risk, skill decuple, mercenary hire, class-specialist gear, bishop force-loot, weapon elemental damage/procs) vs v398 baseline. Official cadence review (~v408 milestone).  
**Git baseline:** `main` @ `ffdd3f0b` (`fix: dual-wield reach uses shorter weapon range and per-hand damage tooltips`). Worktree: **clean** at review start.  
**Verification:** `make ci-full` — **FAILED** in **29m54s** (2 steps). Step 1/11 file-size ratchet failed pre-refactor (`character_stats_panel.gd` 728 lines) — **fixed post-review** via `character_stats_breakdown.gd` extraction. Step 10/11 client bot: **13 of 102 failed** (market/mercenary/blacksmith cluster); step 11 headless smoke **PASS**. Protocol bot + replay (step 9) **PASS** (~10m40s). Re-run `make ci-full` on current `main` to confirm step 1 green and retest step 10. `make validate-shared` — **PASS** (1,909 checks). `make maintainability` — **PASS** post-refactor.  
**Companion reports:**
- [`backend/20260702_v408-backend.md`](backend/20260702_v408-backend.md) — Go authoritative server
- [`client/20260702_v408-client.md`](client/20260702_v408-client.md) — Godot thin client
- [`extras/20260702_v408-shared-tooling-and-process.md`](extras/20260702_v408-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

v399–v408 shipped **10 focused slices** in an autoloop batch: WS reconnect bot proof (v399), scaled upgrade risk (v400), class skill decuple (v401), tier-4 passive proof (v403), mercenary hire (v404), class-specialist gear (v405), bishop force-loot (v406), weapon elemental damage (v407), and weapon elemental procs (v408). Server authority, protocol v8, and rules-as-data discipline **held throughout**.

**v398 paydown themes mostly advanced:** duplicate scenario IDs closed; v399 reconnect proof closed; client extended failures from v398 claimed fixed in prior `$refactor`. **New blocker at v408 review:** `character_stats_panel.gd` crossed the 600-line new-file ratchet (728 lines) — **`$refactor` extracted `character_stats_breakdown.gd`** during this review/refactor pass.

Coordinator debt is the recurring theme: `sim.go` stable (+13 vs baseline); `main.gd` flat (+1); panel god-files persist. `validate_shared.py` monolith unchanged in structure.

## 2. Scorecard

| Area | Score | Direction vs v398 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority; thin client; protocol v8 unchanged through v408. |
| Architecture (internal cohesion) | 7.6 | ↑ from 7.4 | v400–v408 focused modules; stats breakdown extracted; `main.gd` still coordinator-scale. |
| Technical correctness | 8.1 | ↑ from 7.8 | v399–v408 slices clean; companion proc test gap; ci-full step 1 fail pre-refactor. |
| Test quality | 7.8 | ↑ from 7.5 | Reconnect E2E + runtime tests; v408 proc bot defers rolls to Go tests. |
| Maintainability | 7.9 | ↓ from 8.1 → **8.2 post-refactor** | Ratchet fail at review start; breakdown extraction restores green gate. |
| Documentation | 8.2 | ↑ from 8.0 | v399–v408 as-builts complete; v402 lifecycle gap. |
| Process / SDD | 8.0 | ↑ from 7.8 | Autoloop SDD discipline; ci-full confirmation pending. |
| **Overall** | **8.1** | ↑ from 7.9 | Strong boundaries + focused v399–v408 delivery; coordinator debt remains. |

## 3. Cross-cutting themes

1. **Autoloop batch landed cleanly.** Ten slices, substantive shared/rules + server modules, no protocol bump. SDD trail (spec/plan/as-built) healthy except v402 merge.

2. **v398 reconnect proof closed (v399).** Extended `ws_reconnect_proof` + `test_connection_recovery_runtime.gd` — no longer a merge-gate gap.

3. **Maintainability ratchet caught panel growth.** Dual-wield stats columns pushed `character_stats_panel.gd` past 600 lines — fixed by extracting breakdown/formatting to `character_stats_breakdown.gd`.

4. **Server coordinator debt stable, not shrinking.** `sim.go` phase helpers still in-file; `persistTick` and `repos.go` monoliths open. New domains (`weapon_elemental_procs.go`, `mercenary_character.go`) follow micro-module pattern.

5. **Shared tooling debt stable.** `validate_shared.py` +29 lines; tuning-test audit open; duplicate scenario IDs **closed**.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature batch; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[blocker · Test]** **Triage `make ci-full` client failures (13 extended)** — market/mercenary/blacksmith cluster; improved from 19 at v398 but still blocking full matrix. Rerun: `VERBOSE=1 make bot-client SCENARIO=market_purchase_ui HEADLESS=1`.

2. **[High · Maint]** Extract next **`main.gd` domain cluster** — 6,524 lines vs baseline 6,523 (~0 headroom).

3. **[High · Maint]** Split **`inventory_panel.gd`** (1,536 lines) — largest remaining panel god-file.

4. **[Med · Maint]** **Decompose `validate_shared.cross_checks()`** — ~2,845 lines (`validate_shared.py:201–3046`).

5. **[Med · Test]** **Companion proc integration test** — v408 wiring at `companion_ai.go:534–535` lacks Go test.

6. **[Med · Test]** **Tuning-friendly rule-test audit** — systematic Go/GDScript/Python/bot pin conversion (`PROGRESS.md` backlog).

7. **[future · Maint]** Continue **`sim.go` phase paydown** via `simTickCtx` — movement/combat/projectile helpers still in-file.

8. **[Med · Validation]** Semantic cross-checks: weapon elemental proc `effect_id` ↔ `unique_effects.v0.json`; codex index ↔ live rules.

9. **[Med · Maint]** **Typed bot runtime context** — `run.py` at 4,370-line baseline cap.

10. **[minor · SDD]** Document **v402 merge-into-v401** in lifecycle index.

## 5. What "done well" looks like here

- `make ci-full` green on `main` before next feature batch.
- `main.gd` and panel coordinators trending down via focused extractions quarter-over-quarter.
- `validate_shared.py` cross-checks partitioned; tuning-test audit started.
- Every new panel domain extracted before crossing 600-line new-file target.
- Companion/mercenary combat paths have integration tests matching player paths.

## Verification (detail)

| Check | Result |
|-------|--------|
| `make ci-full` step 9 — protocol bot + replay | **PASS** (~10m40s) |
| `make ci-full` step 10 — client bot `SCENARIO=all` | **FAIL** — 13 of 102 scenarios |
| `make ci-full` step 11 — headless smoke | **PASS** (~1m50s) |
| `make ci-full` step 1 — file-size ratchet | **FAIL** pre-refactor; **PASS** post-refactor |
| **Total `make ci-full`** | **FAIL** — 29m54s (pre-refactor tree) |

### Client extended failures (13)

`market_purchase_ui`, `market_active_offer_ui`, `market_cancel_listing_ui`, `mercenary_recovery_ui`, `my_market_offers_panel`, `buyer_offer_cancel_ui`, `market_trade_receipts`, `mercenary_stats_card`, `mercenary_combat_stats`, `market_notification_badge`, `mercenary_offer_variant_ui`, `blacksmith_renew_item`, `blacksmith_upgrade_risk`

*Evidence: `/tmp/arpg-ci-full-v408-review.log`; run on pre-refactor `ffdd3f0b` tree (step 1 fail since fixed).*

---

## v398 follow-up status

| v398 theme | v408 status |
|------------|-------------|
| ci-full 19 client failures | **Claimed fixed** — re-run in progress |
| `main.gd` extraction | **Open** — +1 vs baseline |
| `inventory_panel.gd` split | **Open** — 1,536 lines (−76 since v398) |
| Reconnect bot proof | **Closed** (v399) |
| Duplicate scenario IDs | **Closed** — `CROSS_TREE_SCENARIO_PAIRS` |
| `validate_shared` split | **Open** — monolith +29 lines |
| Tuning-test audit | **Open** |
| v392–v394 plans | **Closed** |
| `character_stats_panel` ratchet | **New fail → fixed in `$refactor`** |

*Evidence: companion reports; git baseline `ffdd3f0b`; comparison to [`20260701_v398-overview.md`](20260701_v398-overview.md).*
