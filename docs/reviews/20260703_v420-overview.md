# arpg-dev — Engineering review at slice **v420** (overview)

**Date:** 2026-07-03  
**Reviewer:** Cursor agent — sequential backend/client/extras audits on `main` @ `dd037a16`.  
**Scope:** Whole repo — v409–v420 autoloop batch (class skill progression: training doll through build branches) vs v408 baseline. Official cadence review (~v420 milestone; 12 slices since v408).  
**Git baseline:** `main` @ `dd037a16` (`fix: stabilize elite objective scenario movement with walk timeouts`). Worktree: **clean**.  
**Verification:** `make ci-full` — **PASS** in **23m35s** (all 11 steps). Protocol bot + replay **PASS** (132 scenarios). Client bot **PASS** (103 scenarios). Headless smoke **PASS**. `make maintainability` — **PASS** (35 grandfathered files, 66,141 lines). `make validate-shared` — **PASS** (prior gate; not re-run at review write).  
**Companion reports:**
- [`backend/20260703_v420-backend.md`](backend/20260703_v420-backend.md) — Go authoritative server
- [`client/20260703_v420-client.md`](client/20260703_v420-client.md) — Godot thin client
- [`extras/20260703_v420-shared-tooling-and-process.md`](extras/20260703_v420-shared-tooling-and-process.md) — contracts, Python tooling, docs/SDD

---

## 1. Executive summary

v409–v420 shipped **twelve focused slices** in an autoloop batch: training doll (v409), polearm icons (v410), skill-tree layout (v411), class build pacing (v412), skill engine (v413), survival skills (v414), specialist gear (v415), per-skill affix rolls (v416), rank scaling (v417), synergies (v418), prerequisite layout (v419), and build branches (v420). Server authority, protocol v8, and rules-as-data discipline **held throughout**.

**v408 blockers closed:** `make ci-full` is green (was failing on file-size ratchet + 13 client extended scenarios). Scenario alignment fixes restored v420 contracts (`max_rank` 10, class stat growth floors, mercenary defend stance, elite objective walk order). v408 companion proc test gap **closed** in Go.

**Recurring theme:** coordinator debt stable, not shrinking — `sim.go` (+9), `rules.go` (+16), `main.gd` (+15), `run.py` (+23) all within ratchet allowances but trending up every autoloop pass. New domains continue the micro-module pattern (`survival_skills.go`, `skill_synergies.go`, `skill_rank_scaling.go`).

## 2. Scorecard

| Area | Score | Direction vs v408 | Evidence |
|------|-------|-------------------|----------|
| Architecture (boundaries) | 9.0 | → stable | Server authority; thin client; protocol v8 unchanged v409–v420. |
| Architecture (internal cohesion) | 7.7 | ↑ from 7.6 | Skill modules extracted; `main.gd`/`sim.go`/`rules.go` still dominate. |
| Technical correctness | 8.4 | ↑ from 8.1 | `make ci-full` green; companion proc test; survival/synergy wiring tested. |
| Test quality | 8.1 | ↑ from 7.8 | 132+103 bot scenarios pass; 100 smoke gates; movement `timeout_s`. |
| Maintainability | 7.9 | ↓ from 8.2 | Ratchet green but coordinators not shrinking; panel/run.py at ceiling. |
| Documentation | 8.3 | ↑ from 8.2 | v409–v420 SDD trail complete; v420 batch spec drift. |
| Process / SDD | 8.1 | ↑ from 8.0 | Autoloop discipline; review now landed at v420. |
| **Overall** | **8.2** | ↑ from 8.1 | Strong delivery + recovered full matrix; structural debt is the ceiling. |

## 3. Cross-cutting themes

1. **Class skill batch landed cleanly.** Twelve slices, substantive `skills.v0.json` growth (~70 skills), no protocol bump. Go micro-modules + extended bot proofs per slice.

2. **Full matrix recovered.** v408 had 13 client extended failures and a ratchet fail; v420 baseline passes 235 bot scenarios + unit gates in ~24m. Refactor commits aligned scenarios with rank-10 catalog and defend default stance.

3. **Movement hygiene improved.** `move_until_in_range` gained optional `timeout_s`; elite objective scenario walks to chest before killing pack leaders — prevents 20-minute hangs.

4. **Server coordinator debt stable, not shrinking.** `sim.go` phase helpers still in-file; `rules.go` validation monolith grew with every skill slice.

5. **Shared tooling debt stable.** `validate_shared.py` cross-check monolith unchanged in structure; `run.py` at ratchet ceiling; tuning-test audit still open.

## 4. Top 10 recommendations

Tags: **[blocker]** = before next feature batch; **[minor]** = safe `$refactor`; **[future]** = multi-slice.

1. **[High · Maint]** Extract next **`main.gd` domain cluster** — 6,539 lines (+15 vs v408); `_apply_delta` hub (`main.gd:1281+`).

2. **[High · Maint]** Decompose **`rules.go` validation** — 3,237 lines (+16); extend survival-skill delegation pattern.

3. **[High · Maint]** Split **`inventory_panel.gd`** (1,527 lines) — largest remaining panel god-file.

4. **[Med · Maint]** **Decompose `validate_shared.cross_checks()`** — ~2,900 lines (`validate_shared.py:201–3133`).

5. **[Med · Maint]** Continue **`sim.go` phase paydown** via `simTickCtx` — movement/combat/projectile helpers still in-file.

6. **[Med · Test]** **Tuning-friendly rule-test audit** — systematic Go/GDScript/Python/bot pin conversion (`PROGRESS.md` backlog).

7. **[Med · Test]** Refresh **`test_coop_client.gd`** fixtures — `max_rank: 5` stale vs rank-10 rules.

8. **[Med · SDD]** Reconcile **v420–v424 spec** with single-slice delivery — update spec or lifecycle for v421–v424.

9. **[Med · Maint]** **Typed bot runtime context** — `run.py` at 4,393-line baseline cap (+23).

10. **[minor · CI]** Unit test for **`movement_runtime.timeout_s`**; document scenario timeout pairing policy.

## 5. What "done well" looks like here

- `make ci-full` green on `main` before each feature batch (achieved at v420).
- `main.gd`, `sim.go`, and `rules.go` trending down via focused extractions quarter-over-quarter.
- `validate_shared.py` cross-checks partitioned; tuning-test audit started.
- One representative class-skill scenario in CI pack if extended-only proofs prove fragile under catalog churn.
- Every new panel domain extracted before crossing 600-line new-file target.

## Verification (detail)

| Check | Result |
|-------|--------|
| `make ci-full` step 9 — protocol bot + replay | **PASS** (132 scenarios) |
| `make ci-full` step 10 — client bot `SCENARIO=all` | **PASS** (103 scenarios) |
| `make ci-full` step 11 — headless smoke | **PASS** (~2m13s) |
| `make ci-full` step 1 — file-size ratchet | **PASS** |
| `make maintainability` | **PASS** — 35 files, 66,141 lines |
| **Total `make ci-full`** | **PASS** — 23m35s |

*Evidence: `/tmp/arpg-ci-full3.log` on `dd037a16` tree.*

---

## v408 follow-up status

| v408 theme | v420 status |
|------------|-------------|
| ci-full 13 client failures | **Closed** — 103/103 pass |
| File-size ratchet (`character_stats_panel`) | **Closed** — stable at 547 + breakdown |
| `main.gd` extraction | **Open** — +15 vs v408 |
| `inventory_panel.gd` split | **Open** — 1,527 lines |
| Reconnect bot proof | **Closed** (v399) |
| Duplicate scenario IDs | **Closed** |
| `validate_shared` split | **Open** — monolith +21 lines |
| Tuning-test audit | **Open** |
| Companion proc integration test | **Closed** |
| `sim.go` phase paydown | **Open** — +9 vs baseline |

*Evidence: companion reports; git baseline `dd037a16`; comparison to [`20260702_v408-overview.md`](20260702_v408-overview.md).*
