# arpg-dev - Backend (Go server) review at slice **v200**

**Date:** 2026-06-15
**Scope:** Go authoritative simulation, realtime/session path, persistence pressure points, and v191-v200 repairs through the progress-doc compaction gate.
**Baseline:** `main` at `310b67d4` (`docs: add v200 engineering review`). Worktree was clean before the requested deletion/regeneration of the previous v200 review files; during this review run the only pre-existing dirt was those deleted review files.
**Stats:** 136 Go files; 54 Go test files; largest backend files: `server/internal/game/game_test.go` 7,896 lines, `server/internal/game/sim.go` 6,606 lines, `server/internal/game/rules.go` 3,250 lines, `server/internal/store/repos.go` 2,321 lines.
**Overview:** [`../20260615_v200-overview.md`](../20260615_v200-overview.md)

---

## Summary

The backend is review-green again after the v200 repair commits. `make ci` passed in 7m44s, including the file-size ratchet, Go tests, determinism lint, protocol bot, replay, client bot, and Godot smoke. The earlier Rage/Ranger skill prerequisite regression is repaired in shared data and now covered by both Go/client expectations and `tools/validate_skills.py`.

The main backend risks are no longer immediate correctness blockers. They are review-follow-up items: companion/elite-minion follow tuning remains hardcoded in Go, monster-rarity validation still mixes expected tuning constants into the runtime loader, and large backend coordinators remain expensive to touch.

## 1. Architecture

- **[Strength] Authoritative gameplay remains server-owned.** Companion, mercenary, affix, skill, loot, and progression behavior continue to resolve in the Go sim; the green protocol bot suite confirms player-facing flows still use the server boundary.
- **[Strength] Skill prerequisite drift was repaired at the shared-rule layer.** Rage now has no skill prerequisite (`shared/rules/skills.v0.json:341`), Volley requires Piercing Shot (`shared/rules/skills.v0.json:843`), and Split Arrow is tier 3 requiring Volley (`shared/rules/skills.v0.json:946`). This matches backend Ranger tests (`server/internal/game/ranger_skills_test.go:194`).
- **[Med] Companion and elite-minion movement tuning is still code-owned.** `companionAssistRadius`, `companionFollowDistance`, and `companionFollowStopRadius` are Go constants (`server/internal/game/companion_ai.go:5`) used in spawn, targeting, and follow behavior (`server/internal/game/companion_ai.go:245`, `server/internal/game/companion_ai.go:343`, `server/internal/game/companion_ai.go:438`). Elite minions reuse the same values (`server/internal/game/elite_minion_ai.go:45`, `server/internal/game/elite_minion_ai.go:61`).

## 2. Technical

- **[Strength] CI is green at the review baseline.** `make ci` passed in 7m44s after the review-gate repairs.
- **[Strength] Determinism remains guarded.** CI passed determinism lint, replay for the final protocol bot session, and all Go tests.
- **[Med] Monster-rarity validation still pins tuning in Go.** `validateMonsterRarities` hardcodes expected ids, weights, offsets, visual scales, and colors (`server/internal/game/rules.go:2281`). That protects current content but makes ordinary tuning changes require Go edits.

## 3. Maintainability

- **[Strength] File-size ratchet is green again.** The baseline refresh in `93cbc261` brought `main.gd`, `test_coop_client.gd`, `rules.go`, and `visual_capture.gd` back under the explicit grandfathered baseline gate.
- **[Med] Backend coordinator size remains the biggest long-term cost.** `game_test.go`, `sim.go`, and `rules.go` are still the three largest Go files. Future class/skill/rule work should create focused tests/helpers instead of adding broad assertions to these files.
- **[Low] Store/replay files remain large but stable.** `server/internal/store/repos.go`, `server/internal/store/store_test.go`, and `server/internal/replay/replay_test.go` are still over the 600-line target, but they did not regress in this gate.

## 4. Documentation

- **[Strength] The v200 progress dashboard compaction reduces task-startup load.** Current status now lives in the slim `PROGRESS.md`; historical lifecycle and shipped notes moved under `docs/progress/`.
- **[Low] Backend review handoff should keep the skill-rule repair visible.** The next backend-affecting class/skill slice should preserve the new validation expectations in `tools/validate_skills.py`.

## Top 5 backend refactors

1. Move companion and elite-minion follow/assist tuning into shared rules while preserving current values.
2. Convert `validateMonsterRarities` expected ids/weights/colors/scales into an explicit golden/schema-owned validation path.
3. Extract the next touched skill/class test cluster out of `server/internal/game/game_test.go`.
4. Keep new companion/mercenary persistence out of `server/internal/store/repos.go` unless paired with a focused split.
5. Add semantic Go/Python validation for any future cross-class skill prerequisite exception.

*Evidence: `make ci` passed on 2026-06-15 in 7m44s; current line stats from `wc -l`; code references from `nl -ba` and `rg`.*
