# arpg-dev - Backend (Go server) review at slice **v232**

**Date:** 2026-06-16
**Scope:** Go authoritative simulation, resource wallet/material pickup, mystery seller set/unique eligibility, market cancellation, mercenary recovery support, realtime/replay, persistence, and backend maintainability after the v226-v232 `$autoloop` queue.
**Baseline:** `main` at `cfae54ca` (`docs: mark v226-v232 ci green`). Worktree was clean before this review started.
**Stats:** 156 Go files; 61 Go test files; largest backend files: `server/internal/game/game_test.go` 7,911 lines, `server/internal/game/sim.go` 6,572 lines, `server/internal/game/rules.go` 3,306 lines, `server/internal/store/repos.go` 2,340 lines.
**Overview:** [`../20260616_v232-overview.md`](../20260616_v232-overview.md)

---

## Summary

The backend remains green at v232. The v226-v232 batch landed mostly presentation-facing features,
but the server still picked up real authoritative work: wallet-resource pickup and snapshot flow,
set/unique mystery eligibility, market cancellation, and mercenary recovery data. The final batch
`make ci` passed after v232, and current focused review checks passed `make validate-shared`,
`cd server && go test ./... && go vet ./...`, and `make maintainability`.

The main backend risks are the same durable ones: companion/elite movement tuning remains in Go
constants, monster-rarity validation still pins tunable catalog values in `rules.go`, and broad
coordinators remain under ratchet pressure.

## 1. Architecture

- **[Strength] Server authority held through resource and economy changes.** Wallet resources are
  recognized and removed by the sim, not by the client (`server/internal/game/resource_wallet.go:3`,
  `server/internal/game/resource_wallet.go:19`), and persisted with store-level add/spend methods
  (`server/internal/store/account_resources.go:42`, `server/internal/store/account_resources.go:68`,
  `server/internal/store/account_resources.go:91`).
- **[Strength] Market cancellation stayed in store/http ownership.** Listing cancellation locks and
  restores seller stash state in the store path (`server/internal/store/market_repo.go:100`,
  `server/internal/store/market_repo.go:112`, `server/internal/store/market_repo.go:124`), while the
  client only emits cancel intent payloads.
- **[Med] Companion and elite-minion movement tuning is still code-owned.** Follow/assist distances
  are constants (`server/internal/game/companion_ai.go:6`) used for companion targeting and follow
  movement (`server/internal/game/companion_ai.go:245`, `server/internal/game/companion_ai.go:353`,
  `server/internal/game/companion_ai.go:456`) and reused by elite minions
  (`server/internal/game/elite_minion_ai.go:45`, `server/internal/game/elite_minion_ai.go:56`).

## 2. Technical

- **[Strength] Backend checks are green.** Current review checks passed `cd server && go test ./... &&
  go vet ./...`; the full v226-v232 batch `make ci` also passed after the selected queue completed.
- **[Strength] Determinism remains guarded by process and CI.** `PROGRESS.md` still documents seeded
  RNG, stable ordering, and determinism lint as invariants (`PROGRESS.md:152`).
- **[Med] Monster-rarity validation still duplicates tunables in Go.** `validateMonsterRarities`
  hardcodes expected IDs, weights, offsets, visual scales, and colors (`server/internal/game/rules.go:2306`).
  That protects current content but makes ordinary tuning/catalog edits require Go changes.

## 3. Maintainability

- **[Strength] Backend coordinators are not growing unchecked.** `sim.go` is now 6,572 lines, down
  from 6,658 at the v223 review, and the ratchet passed.
- **[Med] Large-file debt remains concentrated.** `game_test.go`, `sim.go`, `rules.go`, `repos.go`,
  and `store_test.go` remain above the 600-line target and grandfathered in the ratchet
  (`.maintainability/file-size-baseline.tsv:17`, `.maintainability/file-size-baseline.tsv:22`,
  `.maintainability/file-size-baseline.tsv:30`).
- **[Low] Session snapshot resource flow still sits in broad store code.** Resource wallet snapshot
  persistence is inside `repos.go` (`server/internal/store/repos.go:1861`, `server/internal/store/repos.go:2001`,
  `server/internal/store/repos.go:2206`), which is acceptable for now but should be split when next touched.

## 4. Documentation

- **[Strength] Backend slice proof is complete.** v226-v232 as-built notes now all point at the
  completed batch CI result.
- **[Med] Review follow-up should stay explicit about behavior versus data migration.** Moving
  companion/elite tuning into shared rules can preserve current behavior, but it still touches
  gameplay ownership and should be planned carefully.

## Top 5 backend refactors

1. Move companion and elite-minion follow/assist tuning into shared rules with current-value parity
   and rule-derived tests.
2. Convert monster-rarity expected weights/colors/scales from Go literals into named golden or
   schema-owned validation.
3. Split the next touched assertion cluster out of `server/internal/game/game_test.go`.
4. Keep resource/session snapshot persistence out of `repos.go` when it is next touched.
5. Continue using focused package tests for market/resource wallet changes before full CI.

*Evidence: current checks listed in the overview; current stats from `find`, `wc -l`, and `rg`; code references from `nl -ba`.*
