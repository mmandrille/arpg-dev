# arpg-dev — Backend (Go server) review at slice **v334**

**Date:** 2026-06-25
**Scope:** Go authoritative server — `server/internal/game`, `realtime`, `store`, `replay`. Covers slices v332–v334 plus post-v331 stabilization (navigation, CI, bot routing).
**Baseline:** `main` at `9d02179b` (`fix: restore bot walk routing for open lab worlds and vendor hub bounds`). Worktree has uncommitted WIP on wall-floor shader polish (`main.gd`, bot scenarios, `wall_floor_lab_nav_test.go`).
**Stats:** 159 `server/internal/game/*.go` files (non-test); top files: `sim.go` 6,632, `rules.go` 3,312, `game_test.go` ~7,900+; `go test ./...` green (game package ~246s).
**Overview:** [`../20260625_v334-overview.md`](../20260625_v334-overview.md)

---

## Summary

v332–v334 closed the v331 review blocker: **`go test ./...` is green** after navigation fixes (`buildBlockedFn` cell-center wall AABBs, `planPath` goal-cell allowance, dead-end escape from floating-point drift), golden seed updates, and ranged-positioning test repairs. v333 added corridor-gap spawn exclusion and elite chest clustering in room-layout dungeons. v334 added data-driven ranged-retreat engagement gating (`ranged_retreat_min_melee_engagement_seconds` in `main_config.v0.json`). Determinism lint and `go vet` remain clean.

The structural debt from v331 is **unchanged in direction**: `sim.go` grew to 6,632 lines (+91 since v331) and `rules.go` remains at 3,312 lines with ~80 type definitions still centralized. No net coordinator extraction landed in this window despite new focused files (`dungeon_room_layout.go`, ranged positioning tests).

---

## 1. Architecture

**[Strength]** Handler registry discipline intact. New gameplay tuning (ranged retreat gate) flows through `rules.go` validation and monster AI without new `applyInput` cases.

**[Strength]** v333 room-spawn-awareness keeps spawn logic server-side: corridor exclusion zones are AABBs derived from divider gaps; elite chests sample near pack leaders within `room_cluster_radius`. No client authority leak.

**[Strength]** v334 ranged-retreat-engagement is a clean server-owned timer: monsters with `preferred_min_range` only retreat after `ranged_retreat_min_melee_engagement_seconds` in melee range. Timer resets on range exit. Tuning is data-driven in `main_config.v0.json`.

**[Med]** `sim.go` at 6,632 lines continues absorbing tick-path logic. v332 navigation fixes touched `movement.go`, `pathfind.go`, and `auto_nav.go` correctly, but the coordinator itself did not shrink.

---

## 2. Technical

**[Strength] Test suite green.** `cd server && go test ./...` passes (2026-06-25). This resolves the v331 **[blocker]** finding (7 `game` + 1 `replay` failures). Key fixes landed across v332 commits: collision/pickup/door/obstacle tests, teleporter replay golden seed change, ranged clear-shot validation.

**[Strength] Determinism hygiene.** `make lint-determinism` OK; zero `time.Now()` / bare `math/rand` in hot-path `game/` files. `go vet ./...` clean.

**[Strength] Navigation regression class addressed.** `buildBlockedFn` now uses cell-center for wall AABBs; `planPath` allows goal-cell entry; zero-movement steps are discarded to prevent stall loops. Documented in v332 as-built.

**[Low]** `wall_floor_lab_nav_test.go` is untracked WIP in the worktree — not yet part of CI. Should land with the wall-floor shader slice or as a focused nav regression test.

---

## 3. Maintainability

**[Med]** `sim.go`: 6,632 lines (baseline likely still ~6,541 + allowance). Net growth without documented payback.

**[Med]** `rules.go`: 3,312 lines, 80 `type` definitions. `dungeon_generated_types.go`, `passive_skill_rules.go`, and `shop_views.go` show the extraction pattern works, but the main rules loader file is not shrinking.

**[Strength]** New domain files stay focused: `dungeon_room_layout.go`, ranged positioning in existing monster modules, movement helpers in `movement.go`.

**[Low]** `handlers.go` within +25 drift cap per v331; no new breach observed in this window.

---

## 4. Documentation

**[Strength]** ADR-0015 documents movement-speed formula boundary (v332). Prevents silent cross-language contract drift.

**[Strength]** v332–v334 as-built docs exist with focused verification commands.

---

## Top 5 backend refactors

1. **[minor · Maint]** Extract `DungeonGenerationRules` type cluster and validators from `rules.go` into `dungeon_generation_rules.go` (~200 lines). Low coupling; mirrors existing `shop_views.go` split.

2. **[minor · Maint]** Begin typed tick-context struct for `sim.go` per-tick dispatch — enables incremental extraction without determinism risk. Start with a read-only view struct passed to one handler cluster.

3. **[future]** Full `sim.go` coordinator paydown — multi-slice investment; not a one-shot `$refactor` task.

4. **[minor · Test]** Commit `wall_floor_lab_nav_test.go` with the wall-floor presentation slice or as standalone nav regression once WIP stabilizes.

5. **[reject]** Re-run v331 pickup/collision failure investigation — resolved in v332.

*Evidence: `go test ./...` output 2026-06-25; `wc -l sim.go rules.go`; `make lint-determinism`; as-built v332/v333/v334; `rules.go:75` ranged retreat field; git log v332–v334.*
