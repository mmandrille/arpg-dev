# arpg-dev — Backend (Go server) review at slice **v337**

**Date:** 2026-06-25
**Scope:** Go authoritative server — `server/internal/game`, `realtime`, `store`, `replay`. Covers v335–v337 paydown slices plus v332–v334 stabilization carry-over.
**Baseline:** `main` at `da9a47b4` (`test: add unit tests for fog presentation validators`). Worktree clean.
**Stats:** 94 `server/internal/game/*.go` files (non-test); top files: `sim.go` 6,632, `game_test.go` ~7,945, `rules.go` 3,049, `handlers.go` 1,454; `go vet ./...` clean; full `go test ./...` not re-run in this review (v334 pass ~246s game package).
**Overview:** [`../20260625_v337-overview.md`](../20260625_v337-overview.md)

---

## Summary

v337 delivered the first slice of sim coordinator paydown: `simTickCtx` in `sim_tick_context.go` centralizes per-tick result accumulation for `TickResultsProfiled`, with `tick_results.go` delegating `resultFor` / `finalizeResults` to the context. Tick semantics are unchanged; v334's green test suite status holds (`go vet` clean; `make lint-determinism` OK per spot checks).

The structural debt from v334 is **unchanged in direction**: `sim.go` remains at 6,632 lines (+5 vs baseline, within +25 allowance) with ~100 `*Sim` methods. `rules.go` is flat at 3,049 lines — dungeon-generation **types** moved to `dungeon_generation_rules.go` (267 lines) in v334, but validators remain in the loader. A new finding: `realtime/runner.go` (~649 lines) has **zero call sites**; production uses `session_loop.go` exclusively.

---

## 1. Architecture

**[Strength]** Handler registry discipline intact. `applyInput` dispatches via `inputHandlers`; new intents add one map entry in `handlers.go`, not a new `case` in `sim.go`.

**[Strength]** v337 tick-context seam is a clean incremental extraction pattern. `simTickCtx` bundles tick scratch state; `TickResultsProfiled` remains the single orchestration entry (`sim_tick_context.go`, `tick_results.go`).

**[Strength]** Deterministic RNG and replay contract unchanged — splitmix64 in `rng.go`; `make lint-determinism` OK.

**[Strength]** Realtime guardrails enforce 10 Hz budget with overload degradation when pathfinding/AI is hot (`tick_guardrails.go`, `session_tick.go`).

**[Strength]** v334 ranged-retreat engagement and v333 room-spawn awareness remain server-authoritative with data-driven tuning in `main_config.v0.json`.

**[Med]** `sim.go` at 6,632 lines continues absorbing tick-path logic. v337 proved the context seam but did not net-shrink the coordinator.

**[Med]** `realtime/runner.go` (~649 lines) appears dead — `newRunner` has no call sites; `hub.go` wires `sessionLoop` only. `runner.doTick` calls `TickResults` (no profiler) vs `session_tick.go` `TickResultsProfiled` — divergent API if revived.

**[Low]** `session_loop.persistTick` remains a large event-type → store dispatch switch (~300+ lines at `session_loop.go:624+`).

---

## 2. Technical

**[Strength]** `cd server && go vet ./...` — **PASSED** (2026-06-25).

**[Strength]** `make lint-determinism` — OK (spot check via maintainability gate).

**[Strength]** v332 navigation regression class remains addressed (`buildBlockedFn` cell-center, `planPath` goal allowance).

**[Strength]** `wall_floor_lab_nav_test.go` committed with v336 wall-floor lab nav proof.

**[Low]** Full `cd server && go test ./...` not re-run in this review (~4 min). v334 evidence: all packages green. No backend code changes since last full run except test-only commits (`da9a47b4`, `2d2607a4`).

**[Low]** PROGRESS.md notes batch `make ci` still pending (protocol-bot 500s + client-bot hang) — integration/env, not Go unit-test failure.

---

## 3. Maintainability

**[Med]** `sim.go`: 6,632 lines (baseline 6,627 + 25). Net +5 since v334; within ratchet.

**[Med]** `rules.go`: 3,049 lines at baseline. Type cluster extracted; ~200 lines of dungeon-gen **validators** still inline.

**[Strength]** New focused files under 600 lines: `sim_tick_context.go` (62), `dungeon_generation_rules.go` (267), `tick_results.go` (89).

**[Med]** `game_test.go`: ~7,945 lines; game package ~230s — primary backend iteration tax.

**[Med]** `repos.go`: 2,333 lines (+18 vs baseline); market/stash paths concentrated.

**[Low]** `handlers.go` at 1,454 (+24 vs baseline) — at +25 drift cap.

**[Strength]** Extraction-coupling ratchet: 0 `helpers=globals()` sites in `game/`.

---

## 4. Documentation

**[Strength]** v332–v334 as-builts and ADR-0015 (movement-speed formula) remain accurate.

**[Strength]** CODEMAP lists `sim_tick_context.go` and `tick_results.go`.

**[Med]** v337 as-built is minimal — documents starter extraction and defers multi-slice paydown; no phase-helper roadmap.

**[Low]** `runner.go` dead-code status undocumented in CODEMAP or ADR.

**[Low]** No single doc mapping `game.Event` types → store mutations (`persistTick` embedded logic).

---

## Top 5 backend refactors

1. **[future · Maint]** Continue `sim.go` paydown via `simTickCtx` — move tick-phase helpers (`applyMovement`, monster AI pass, regen loop) onto context methods or dedicated phase files. v337 proved the seam; highest ROI without determinism risk.

2. **[minor · Maint]** Remove or quarantine `realtime/runner.go` (~649 lines). Zero call sites; duplicates `session_loop` with divergent tick API.

3. **[minor · Maint]** Extract dungeon-generation validators from `rules.go` into `dungeon_generation_rules.go` (or `dungeon_generation_validation.go`). Types already extracted (v334); validators still inline.

4. **[future · Maint]** Split `session_loop.persistTick` event dispatch into per-domain persist helpers. Couples realtime to every economy/stash/market mutation.

5. **[future · Test]** Partition `game_test.go` by domain (combat, dungeon, shop, coop). ~7,945 lines / ~230s slows CI feedback.

**[reject]** Re-open v331 navigation/replay test failures — resolved in v332.

*Evidence: `go vet ./...` 2026-06-25; `wc -l` on key files; `make maintainability`; `rg newRunner server/`; v334 backend review comparison; `docs/as-built/v337_sim-tick-context.md`.*
