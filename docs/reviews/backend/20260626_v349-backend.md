# arpg-dev — Backend (Go server) review at slice **v349**

**Date:** 2026-06-26  
**Scope:** Go authoritative server — v338–v349 delta on `main` (render perf, combat presentation hooks, depth pressure, melee windup, quadruped telegraph, CI stabilization).  
**Baseline:** `main` @ `82422216` (`chore: remove unmanifested client assets and gate orphans in CI`). Worktree: uncommitted edit to `skills/review/SKILL.md` only.  
**Stats:** `game/` 95 source + 69 test `.go` files; `sim.go` 6,641 lines; `handlers.go` 1,454; `rules.go` 3,059; `game_test.go` 7,945; 35 `inputHandlers` intents; 7/14 `server/internal/*` packages with tests.  
**Overview:** [`../20260626_v349-overview.md`](../20260626_v349-overview.md)

---

## Summary

The authoritative boundary remains sound: intents dispatch through `inputHandlers`, determinism lint covers `game/` hot paths, and v345–v346 added focused extractions (`monster_melee_windup.go`, depth-pressure tests) without protocol changes. **No store/replay/migrations commits** landed between v337 and v349 — persistence surface is stable.

Coordinator debt is unchanged in spirit: `sim.go` grew +14 vs baseline (within ratchet), `simTickCtx` is still a thin seam without phase-helper migration, and **`realtime/runner.go` remains dead code** (649 lines, zero call sites). The v349 review’s **`make ci-full` gate failed** on extended protocol scenarios (12 failures), including long-running unique-effect proofs and coop/loot/dungeon-elite flows — merge-gate `make ci` is green, but the full matrix exposes extended-tier fragility.

## 1. Architecture

**[Strength]** Handler registry discipline intact — `applyInput` uses `inputHandlers` only (`sim.go:1033-1044`); new intents still register in `handlers.go`.

**[Strength]** v337 `simTickCtx` seam preserved (`sim_tick_context.go`, `tick_results.go`); v345 added `advanceMonsterMeleeWindups` hook without crossing the wire.

**[Med]** `sim.go` still owns movement, monster AI, and tick orchestration (~100 methods). Phase helpers (`applyMovement` ~2650+, `advanceMonsterMovement` ~3161+) were not moved onto context — v337 roadmap item still open.

**[Med]** `realtime/runner.go` (649 lines) duplicates session-loop concerns with `TickResults` vs production `TickResultsProfiled` (`session_tick.go:22`). `newRunner` has **zero call sites** — quarantine debt from v337.

**[Strength]** Store/replay untouched v338–v349; `session_loop` + `hub` remain the production WebSocket path.

## 2. Technical

**[Strength]** Determinism posture unchanged: seeded RNG, no wall-clock in `game/`, lint enforced in CI step 5.

**[Strength]** v340 depth-combat-pressure updated `monster_depth_scaling` + `monster_rarity` golden with cross-validation in `validate_shared.py`.

**[Med]** `make ci-full` **failed** step 9/11 with **12 extended protocol scenario failures**, including `resource_support_mobility_unique_effects` (~762s), `dungeon_elite_side_objective`, coop/gold-autopickup, pack aggro, and unique-effect suites. Fast `make ci` pack (36 scenarios) passes — extended matrix is the gap.

**[Low]** `go vet` / full `go test ./...` not separately re-run this review; covered inside `make ci-full` steps 5–6 (passed).

## 3. Maintainability

**[Strength]** All grandfathered coordinators within +25 ratchet: `sim.go` +14, `handlers.go` +24, `rules.go` +10.

**[Med]** `rules.go` dungeon-generation validators still inline (~1685+); types live in `dungeon_generation_rules.go` — v337 extraction item open.

**[Med]** `session_loop.persistTick` remains a ~230-line event→store switch (`session_loop.go:624–854`).

**[Med]** `game_test.go` monolith 7,945 lines — partition deferred since v337.

## 4. Documentation

**[Strength]** v340/v345 combat slices have specs + as-builts; depth pressure golden documented.

**[Med]** v343/v344/v346 presentation slices rely on as-built-only traceability (spec-gate exempt path, but lifecycle table inconsistent for v347).

## Top 5 backend refactors

1. **[future · Maint]** Continue `sim.go` paydown via `simTickCtx` — migrate `applyMovement` / monster AI passes to `sim_tick_*.go` phase files.
2. **[minor · Maint]** Delete or quarantine `realtime/runner.go` — 649 lines, zero callers, divergent tick API.
3. **[minor · Maint]** Extract dungeon-generation validators from `rules.go` into `dungeon_generation_validation.go`.
4. **[future · Maint]** Split `session_loop.persistTick` into per-domain persist helpers (stash, market, death, progression).
5. **[blocker · Test]** Triage extended protocol failures from `make ci-full` — prioritize `resource_support_mobility_unique_effects` budget (762s) and coop/elite/unique suites before next feature batch.

*Evidence: `wc -l`, `.maintainability/file-size-baseline.tsv`, `rg newRunner`, v337 backend report comparison, `/tmp/arpg-ci-full-review.log` 2026-06-26.*
