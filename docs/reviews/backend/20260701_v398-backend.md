# arpg-dev — Backend (Go server) review at slice **v398**

**Date:** 2026-07-01  
**Scope:** Go authoritative server — v385–v398 slices (economy/itemization v390–v397, client-only v398) vs v384 baseline.  
**Baseline:** `main` @ `e0f402e4` (`fix: update set and named unique display name grammar tests`). Worktree: **clean**.  
**Stats:** `sim.go` 6,555 lines (~100 methods); `handlers.go` 1,397 / 39 intents; `session_loop.go` 891; `rules.go` 2,957; `game_test.go` 7,459; `repos.go` 2,201; 276 Go files; 35 grandfathered files / 65,746 lines.  
**Overview:** [`../20260701_v398-overview.md`](../20260701_v398-overview.md)

---

## Summary

The authoritative server boundary remains intact through v398: intents dispatch through `inputHandlers`, production wiring is `hub` → `sessionLoop` only, and v384 paydown themes **held** — dead `runner.go` is gone, `sim.go` shrank ~8 lines since v384 review, monster movement dedup landed, and `make ci-full` was green post-v384 paydown (re-verification in progress for this review).

v390–v397 added economy/itemization depth in **focused modules** (`resource_loot_drops.go`, `leveled_consumable_store.go`, `item_reroll.go`, `affix_names.go`, `item_templates_test.go`) rather than coordinator bloat. v398 is client-only; reconnect uses existing `resume_session_id` / WS resume paths (`server/internal/http/session.go`, `ws_test.go`).

Remaining debt is familiar coordinator scale: `sim.go` still owns movement/combat/projectile tick phases, `session_loop.persistTick` is a large event switch, `repos.go` centralizes ~62 store methods, and `game_test.go` remains a mega-file.

## 1. Architecture

**[Strength]** Authoritative boundary intact — `applyInput` dispatches exclusively via `inputHandlers` (`server/internal/game/sim.go:1072–1084`, `handlers.go:24–66`).

**[Strength]** Production path is `Hub.Run` → `sessionLoop` only. `runner.go` **deleted** — zero references under `server/` (v384 blocker closed).

**[Strength]** v384 perf architecture retained: tick cache, combat budget throttle, persist defer, overload guardrails (`session_tick.go`, `persist_defer.go`, `combat_tick_budget.go`, `tick_guardrails.go`).

**[Strength]** v390–v397 domain extractions follow the micro-module pattern — upgrade shards, leveled consumables, blacksmith flows, archetype templates land outside `sim.go`.

**[High]** `sim.go` still the movement/combat/AI coordinator (~6,555 lines, ~100 methods). Phase helpers not migrated onto `simTickCtx` — `applyMovement` (`sim.go:2684`), `advanceMonsterAttack` (`sim.go:3126`), `advanceProjectiles` (`sim.go:3506`); ctx aggregates results only (`sim_tick_context.go`, `tick_results.go`).

**[Med]** `session_loop.persistTick` remains a ~240-line event/change monolith (`session_loop.go:626–866`).

**[Med]** `repos.go` centralizes implementation despite narrow interface segregation (`interfaces.go:10–80`); ~2,201 lines, 62 methods.

**[Low]** Hub dev-origin policy still wide open (`hub.go:36–40`).

## 2. Technical

**[Strength]** Determinism posture solid: seeded splitmix64 RNG (`rng.go`), no `time.Now()`/`math/rand` in `game/` hot paths, determinism lint enforced (`cmd/determinism-lint/main.go`).

**[Strength]** Persist defer for non-critical ops (shop/progression) wired and tested (`persist_defer.go`, `session_loop_test.go`).

**[Strength]** Replay isolated from realtime; `Reconstruct` from seed + inputs (`replay/replay.go:97–136`).

**[Med]** Combat phase budget hardcoded at 45ms (`combat_tick_budget.go:5–10`); defer trigger is sim-duration-only (`session_tick.go:56–57`).

**[Med]** v397 archetype template ID rename is **breaking** for persisted characters — documented (`docs/as-built/v397_item-archetype-library.md` requires `make db-reset`).

**[Low]** v398 reconnect is client-only; server resume paths pre-exist but no reconnect-specific server extensions.

**[Strength]** v384 ci-full failures recovered and held green per `PROGRESS.md` through v398; v397–v398 did not regress server contracts.

## 3. Maintainability

**[Strength]** Grandfathered backend files at or below ratchet baselines — `sim.go` 6,555 vs baseline 6,542 (within +25 allowance), `game_test.go` 7,459 vs 7,938, `session_loop.go` 891 vs 942.

**[Strength]** `runner.go` removal eliminates ~650 lines of divergent dead code (v384 theme **closed**).

**[Med]** `sim.go` method count unchanged (~100) despite line shrink — extractions partial, not structural decoupling of tick phases.

**[Med]** `rules.go` still ~2,957 lines; validation/load logic not split.

**[Med]** `game_test.go` ~7,459 lines in one `package game` file — `item_templates_test.go` (v397) shows the partition pattern but mega-file persists.

## 4. Documentation

**[Strength]** v390–v397 backend as-builts substantive with verification commands (e.g. `v391_renew-stones.md`, `v397_item-archetype-library.md`).

**[Strength]** `CODEMAP.md` references perf modules; no stale `runner.go` entries.

**[Low]** v396 codex is tooling/client-heavy; v398 is client-only — no server doc gap for v398 itself.

## Top 5 backend refactors

1. **[future · Maint · High]** Continue **`sim.go` phase paydown via `simTickCtx`** — migrate `applyMovement`, `advanceMonsterAttack`, `advanceProjectiles` into `sim_tick_*.go` (`sim.go:2684`, `:3126`, `:3506`; `tick_results.go:17–90`).

2. **[future · Maint · Med]** **Split `session_loop.persistTick`** — 240-line event/change switch (`session_loop.go:626–866`).

3. **[future · Maint · Med]** **Partition `repos.go`** by domain interface (`interfaces.go:14–80`; ~2,201 lines today).

4. **[minor · Maint · Med]** **Partition `game_test.go`** — follow `item_templates_test.go` pattern (~7,459 lines in one file).

5. **[minor · Tech · Med]** **Data-drive or document perf budgets** — `defaultCombatPhaseBudget = 45 * time.Millisecond` (`combat_tick_budget.go:5`); defer trigger sim-only (`session_tick.go:56–57`).

*Evidence: file reads on `main` @ `e0f402e4`; `make maintainability` PASS (35 files, 65,746 lines); comparison to [`20260629_v384-backend.md`](20260629_v384-backend.md).*
