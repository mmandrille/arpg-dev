# arpg-dev — Backend (Go server) review at slice **v408**

**Date:** 2026-07-02  
**Scope:** Go authoritative server — v399–v408 autoloop batch vs v398 baseline.  
**Baseline:** `main` @ `ffdd3f0b` (`fix: dual-wield reach uses shorter weapon range and per-hand damage tooltips`). Worktree: **clean**.  
**Stats:** 295 Go files under `server/`; `sim.go` **6,584** lines; `handlers.go` **1,465** lines (38 intents); `game_test.go` **7,968** lines.  
**Overview:** [`../20260702_v408-overview.md`](../20260702_v408-overview.md)

---

## Summary

The Go server remains a strong authoritative core: intent registry discipline, seeded determinism, and replay isolation all held through v399–v408. New combat/economy domains landed as focused modules (`weapon_elemental.go`, `weapon_elemental_procs.go`, `mercenary_character.go`, `bishop_loot_debug.go`, `item_upgrade_chance.go`) without protocol churn.

**[Strength]** Coordinator debt did not worsen materially — `sim.go` is +13 lines vs ratchet baseline (within +25). **Risks** are unchanged structural debt: phase helpers still in `sim.go`, `persistTick` monolith, `repos.go` bulk, and `game_test.go` at 7,968 lines. v408 companion proc wiring (`companion_ai.go:534–535`) lacks a dedicated integration test.

## 1. Architecture

**[Strength]** Thin dispatch boundary — `applyInput` delegates exclusively via `inputHandlers` (`sim.go:1084–1096`, `handlers.go:24–65`).

**[Strength]** Tick orchestration phased in `tick_results.go`; profiler hooks for perf guardrails (`tick_results.go:10–90`).

**[Strength]** v407–v408 weapon elemental pipeline layered: affix roll (`weapon_elemental.go`), split damage, data-driven procs (`weapon_elemental_procs.go:51–98`), integrated post-physical-hit (`sim.go:1359–1361`).

**[Strength]** Melee reach extracted (`player_melee_reach.go:3–57`); consumed by handlers and sim attack paths.

**[High]** `simTickCtx` remains a result aggregator, not a phase host — `applyMovement`, `advanceMonsterAttack`, `advanceProjectiles` still in `sim.go` (`sim.go:2706`, `:3148`, `:3528`).

**[Med]** `session_loop.persistTick` ~240-line event/change monolith (`session_loop.go:636–876`).

**[Med]** Store interfaces segregated (`interfaces.go:14–90`) but `repos.go` still ~62 methods (2,304 lines).

## 2. Technical

**[Strength]** Determinism posture intact: seeded RNG (`rng.go:5–31`), no `time.Now()` in `game/`, determinism lint on hot paths.

**[Strength]** v400 upgrade chance golden; v408 proc rolls use `s.rng.IntN` (`weapon_elemental_procs.go:109–117`).

**[Med]** Combat phase budget hardcoded 45ms (`combat_tick_budget.go:5–10`).

**[Med]** Companion elemental hits route through player proc path (`companion_ai.go:534–535`) — no integration test.

**[Low]** Hub dev-origin policy still permissive (`hub.go:36–40`).

## 3. Maintainability

| File | Baseline | Actual | Status |
|------|----------|--------|--------|
| `sim.go` | 6,571 | 6,584 | +13 — within +25 |
| `handlers.go` | 1,458 | 1,465 | +7 |
| `game_test.go` | 7,968 | 7,968 | at baseline |
| `repos.go` | 2,315 | 2,304 | −11 |

Grandfathered: 35 files, 65,696 baseline lines. Extraction coupling: 0 `helpers=globals()` sites.

**[Med]** `mercenary_character.go` (589) and `bishop_loot_debug.go` (508) approaching 600-line new-file ceiling.

## 4. Documentation

**[Strength]** v399–v408 slices have specs, plans, and as-builts with verification commands.

**[Strength]** `docs/CODEMAP.md` updated for combat domain modules.

**[Low]** No v402 lifecycle row (merged into v401).

---

## Top 5 backend refactors

1. **[future · Maint]** Continue `sim.go` phase paydown via `simTickCtx` — extract movement/combat/projectile helpers into `sim_tick_*.go`.
2. **[future · Maint]** Split `session_loop.persistTick` (`session_loop.go:636–876`) into focused persist modules.
3. **[future · Maint]** Finish `repos.go` domain partition following `market_repo.go` precedent.
4. **[minor · Test]** Partition `game_test.go`; add companion proc integration test for v408 gap.
5. **[minor · Tech]** Data-drive combat phase budget (`combat_tick_budget.go:5`).

*Evidence: file reads at `ffdd3f0b`; comparison to [`20260701_v398-backend.md`](20260701_v398-backend.md).*
