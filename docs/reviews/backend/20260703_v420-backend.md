# arpg-dev — Backend (Go server) review at slice **v420**

**Date:** 2026-07-03  
**Scope:** Go authoritative server — v409–v420 autoloop batch (class skill progression) vs v408 baseline.  
**Baseline:** `main` @ `dd037a16` (`fix: stabilize elite objective scenario movement with walk timeouts`). Worktree: **clean**.  
**Stats:** 317 Go files under `server/`; `sim.go` **6,612** lines; `handlers.go` **1,463** lines (38 intents); `game_test.go` **7,968** lines; 106 `*_test.go` under `internal/game/`.  
**Overview:** [`../20260703_v420-overview.md`](../20260703_v420-overview.md)

---

## Summary

The Go server remains a strong authoritative core through v420. ADR-0001 D2, determinism invariants, and intent-registry discipline held across twelve skill/progression slices. New domains landed as focused modules (`training_doll.go`, `class_progression.go`, `class_skill_engine.go`, `survival_skills.go`, `skill_rank_scaling.go`, `skill_synergies.go`) without a protocol bump.

**[Strength]** v408 companion weapon-proc test gap is **closed** (`TestCompanionWeaponElementalProcOnMeleeHit` in `weapon_elemental_procs_test.go`). **Risks** are unchanged structural debt: `sim.go` and `rules.go` coordinators grew modestly (+9 and +16 vs ratchet baselines); `persistTick`, `repos.go`, and `game_test.go` monoliths remain open.

## 1. Architecture

**[Strength]** Thin dispatch boundary — `applyInput` delegates exclusively via `inputHandlers` (`sim.go:1099–1108`, `handlers.go:26–64`).

**[Strength]** Tick orchestration phased in `tick_results.go`; survival/synergy/rank hooks integrate without crossing the wire (`survival_skills.go`, `skill_synergies.go`, `skill_rank_scaling.go`).

**[Strength]** v409 training doll reuses monster combat pipeline (`training_doll.go`); v420 build-branch actives register through existing skill engine paths.

**[High]** `simTickCtx` remains a scratch aggregator, not a phase host — `applyMovement`, `advanceMonsterAttack`, `advanceProjectiles` still in `sim.go` (`sim.go:2742`, `:3184`, `:3564`).

**[High]** `rules.go` validation/loader monolith at **3,237** lines (+16 vs baseline) — every skill slice touches shared validation.

**[Med]** `session_loop.persistTick` ~240-line event/change monolith (`session_loop.go:636–876`).

**[Med]** Store interfaces segregated (`interfaces.go`) but `repos.go` still **2,304** lines.

## 2. Technical

**[Strength]** Determinism posture intact: seeded RNG (`rng.go`), no `time.Now()` / `math.Rand` in `game/` hot paths; replay re-enters `TickResults`.

**[Strength]** v412–v418 progression contracts tested with rule-derived expectations (`class_progression.go`, `skill_rank_scaling_test.go`, `skill_synergies_test.go`).

**[Med]** Class-skill matrix mostly **extended-bot** gated — merge gate relies on focused Go tests + CI pack, not full skill-tree E2E.

**[Med]** Combat phase budget hardcoded 45ms (`combat_tick_budget.go:5–10`).

**[Low]** Hub dev-origin policy still permissive (`hub.go:36–39`).

## 3. Maintainability

| File | Baseline | Actual | Status |
|------|----------|--------|--------|
| `sim.go` | 6,603 | 6,612 | +9 — within +25 |
| `handlers.go` | 1,458 | 1,463 | +5 |
| `rules.go` | 3,221 | 3,237 | +16 |
| `game_test.go` | 7,968 | 7,968 | at baseline |
| `repos.go` | 2,315 | 2,304 | −11 |

Grandfathered: 35 files, 66,141 lines. Extraction coupling: 0 `helpers=globals()` sites.

**[Med]** `mercenary_character.go` (596), `rogue_skills.go` (596), `survival_skills.go` (468), `companion_ai.go` (550) — approaching 600-line ceiling for new modules.

## 4. Documentation

**[Strength]** v409–v420 slices have specs, plans, and as-builts with verification commands.

**[Strength]** `docs/CODEMAP.md` updated for combat/skill domains.

**[Low]** v420 spec still titles v420–v424 batch while delivery landed in one slice — lifecycle/spec drift (see extras report).

---

## Top 5 backend refactors

1. **[High · Maint]** Continue `sim.go` phase paydown via `simTickCtx` — extract movement/combat/projectile helpers into `sim_tick_*.go` (`sim.go:2742`, `:3184`, `:3564`).
2. **[High · Maint]** Decompose `rules.go` validation — extend v414 survival delegation pattern into `skill_rules_validation.go` or domain splits; loader stays thin.
3. **[Med · Maint]** Split `session_loop.persistTick` (`session_loop.go:636–876`) into per-`Op*` persist modules.
4. **[Med · Test/Maint]** Partition `game_test.go` (7,968 lines) — move shop/stash/travel clusters to domain files.
5. **[Med · Maint]** Finish `repos.go` domain partition following `market_repo.go` precedent.

*Evidence: file reads at `dd037a16`; comparison to [`20260702_v408-backend.md`](20260702_v408-backend.md).*
