# arpg-dev - Client (Godot 4 / GDScript) review at slice **v200**

**Date:** 2026-06-15
**Scope:** Godot thin-client protocol application, status/companion presentation, client bot coverage, and v191-v200 visible changes.
**Baseline:** `main` at `9136e410` (`feat: v200: progress doc compaction`); worktree clean before review docs were written. `$refactor` ran first and produced `32c9d347 refactor: derive sanctuary visual radius from rules`.
**Stats:** 108 GDScript files; 29 GDScript tests; 45 client bot scenarios; largest client files: `client/scripts/main.gd` 6,521 lines, `client/scripts/bot_scenario_runner.gd` 1,642 lines, `client/scripts/inventory_panel.gd` 1,536 lines, `client/tests/test_coop_client.gd` 1,519 lines.
**Overview:** [`../20260615_v200-overview.md`](../20260615_v200-overview.md)

---

## Summary

The client remains mostly healthy as a thin renderer/input layer, and the pre-review `$refactor` closed the v190 Sanctuary radius drift. The current client gate is still red because class skill progression data from the server makes `client_skill_points_and_magic_bolt` fail: the client correctly renders Rage as not spendable, but that now contradicts the established scenario expectation.

## 1. Architecture

- **[Strength] Sanctuary presentation now reads shared rule data.** `main.gd` derives the radius through `SkillRulesLoader` (`client/scripts/main.gd:1736`) and passes it to the status marker (`client/scripts/main.gd:1129`, `client/scripts/main.gd:4970`); the marker builds dome and ground meshes from the supplied radius (`client/scripts/player_status_effect_markers.gd:238`, `client/scripts/player_status_effect_markers.gd:260`).
- **[Strength] The client still follows protocol state/events.** Status-effect presentation remains event/state driven, and the v200 refactor did not add client-owned gameplay decisions.
- **[Med] `main.gd` remains the main presentation sink.** Status-effect routing, debug-state assembly, companion updates, quest/minimap sync, and boss telegraphs all still concentrate in one 6,521-line file.

## 2. Technical

- **[High] One client bot scenario fails because server-authored skill progression drifted.** `make ci` reports `client_skill_points_and_magic_bolt` failed: the scenario expected Rage `can_spend=true`, but the client received Rage `can_spend=false`. This matches the shared rule drift in `shared/rules/skills.v0.json:362`.
- **[Strength] Godot unit/smoke coverage is green.** `make client-unit` passed during `$refactor`, the focused `test_status_effect_presentation.gd` passed with 37 assertions, and CI's final Godot headless smoke step passed.
- **[Strength] Most client bot coverage is green.** CI reports 44 of 45 client bot scenarios passed; the failure is concentrated in skill progression rather than rendering/bootstrap.

## 3. Maintainability

- **[High] Client files are over the active file-size ratchet.** CI reports `client/scripts/main.gd` at 6,521 lines versus baseline 6,351 plus 25, and `client/tests/test_coop_client.gd` at 1,519 lines versus baseline 1,398 plus 25.
- **[Med] The new companion bar is useful but adds another presentation surface near `main.gd`.** `client/scripts/companion_bar.gd` owns row icons and countdown rendering; future companion UI should keep updates out of `main.gd`.

## 4. Documentation

- **[Strength] Visual-only handoff is now better for Sanctuary.** The v200 refactor added a rule-derived radius assertion to `client/tests/test_status_effect_presentation.gd`, making future visual drift easier to catch.
- **[Med] The progress archive move created likely broken relative Markdown links.** `docs/progress/slice-lifecycle.md:20` still links to `docs/specs/...` from inside `docs/progress/`, which resolves to `docs/progress/docs/specs/...` in normal Markdown link semantics.

## Top 5 client refactors

1. Fix the shared skill rows, then rerun `make client-unit`, `make client-smoke`, and client bot scenario `client_skill_points_and_magic_bolt`.
2. Split the next touched presentation/debug-state sync path out of `client/scripts/main.gd`.
3. Reduce or split `client/tests/test_coop_client.gd` before adding new co-op/companion assertions.
4. Keep companion UI work in `companion_bar.gd` or focused helpers rather than routing more logic through `main.gd`.
5. Add a progress-link checker or fix archive links after the v200 progress compaction.

*Evidence: `make ci` failed in client bot step 10/11 with 44 passed and 1 failed; `make client-unit` passed during `$refactor`; line counts from `wc -l`.*
