# arpg-dev - Client (Godot 4 / GDScript) review at slice **v200**

**Date:** 2026-06-15
**Scope:** Godot thin-client protocol application, status/companion presentation, client bot coverage, and v191-v200 UI-visible repairs.
**Baseline:** `main` at `310b67d4` (`docs: add v200 engineering review`). Worktree was clean before the requested deletion/regeneration of the previous v200 review files; during this review run the only pre-existing dirt was those deleted review files.
**Stats:** 108 GDScript files; 29 GDScript tests; 45 client bot scenarios; largest client files: `client/scripts/main.gd` 6,521 lines, `client/scripts/bot_scenario_runner.gd` 1,642 lines, `client/scripts/inventory_panel.gd` 1,536 lines, `client/tests/test_coop_client.gd` 1,519 lines.
**Overview:** [`../20260615_v200-overview.md`](../20260615_v200-overview.md)

---

## Summary

The client is green at v200. The previously failing `client_skill_points_and_magic_bolt` scenario now passes, and CI reports 45/45 client bot scenarios plus the full Godot headless smoke suite passing. The v190 Sanctuary drift is also closed: the client now derives the Sanctuary dome radius from shared skill rules instead of copying a presentation constant.

The client remains a thin renderer/input layer. Its main risk is maintainability pressure in `main.gd` and `test_coop_client.gd`, both of which required a refreshed file-size baseline after recent feature work.

## 1. Architecture

- **[Strength] Sanctuary presentation reads shared skill data.** `main.gd` derives the radius with `SkillRulesLoader` (`client/scripts/main.gd:1736`) and passes it into the marker sync (`client/scripts/main.gd:1129`, `client/scripts/main.gd:4970`).
- **[Strength] Client bot coverage confirms the repaired skill progression flow.** CI now passes `client_skill_points_and_magic_bolt`, the scenario that failed in the previous review run.
- **[Med] `main.gd` remains a broad presentation coordinator.** It still owns status effect routing, quest/minimap sync, bot debug state, boss telegraphs, and entity presentation (`client/scripts/main.gd:6099`, `client/scripts/main.gd:6145`).

## 2. Technical

- **[Strength] Client verification is green.** CI passed all 45 client bot scenarios and the final Godot smoke suite. A focused status-effect presentation test also covered the Sanctuary radius change during `$refactor`.
- **[Strength] The client faithfully reflects server-authored skill state.** The skill progression regression was fixed in shared rules; the client did not require authority-side shortcuts.
- **[Low] Sanctuary marker construction still has a fallback default.** `sync_sanctuary_effect` defaults to `5.0` for direct callers (`client/scripts/player_status_effect_markers.gd:43`). Current gameplay paths pass the rule-derived radius, but future direct callers should do the same.

## 3. Maintainability

- **[Med] Large client files remain managed debt.** `client/scripts/main.gd` and `client/tests/test_coop_client.gd` are now explicitly grandfathered at their current larger sizes (`.maintainability/file-size-baseline.tsv:7`, `.maintainability/file-size-baseline.tsv:13`). The ratchet is green, but future edits should shrink or split touched areas.
- **[Strength] Presentation helpers continue to absorb specific domains.** `companion_bar.gd`, status-effect marker helpers, quest/minimap helpers, and panel scripts keep some UI domains out of `main.gd`.
- **[Low] Client bot runner remains large but stable.** `client/scripts/bot_scenario_runner.gd` is 1,642 lines; future bot steps should continue landing in helper scripts.

## 4. Documentation

- **[Strength] Visual proof names remain clear in the scenario catalog.** `docs/progress/scenario-catalog.md` names client and protocol scenarios for skill, boss, market, and companion coverage.
- **[Med] Progress archive links likely need repair.** `docs/progress/slice-lifecycle.md:20` contains `docs/specs/...` links even though the file now lives under `docs/progress/`.

## Top 5 client refactors

1. Split the next touched presentation/debug-state path out of `client/scripts/main.gd`.
2. Reduce or split `client/tests/test_coop_client.gd` before adding new co-op/companion assertions.
3. Keep new companion UI behavior in `companion_bar.gd` or focused helpers.
4. Add link validation for moved progress archives or repair their relative paths.
5. Preserve rule-derived status-effect presentation patterns for future area VFX.

*Evidence: `make ci` passed with 45/45 client bot scenarios and Godot smoke; line counts from `wc -l`; code references from `nl -ba`.*
