# arpg-dev — Client (Godot 4 / GDScript) review at slice **v334**

**Date:** 2026-06-25
**Scope:** Godot thin client — `client/scripts/`, `client/tests/`. Covers v332–v334 plus post-v331 bot/CI stabilization and in-progress wall-floor shader WIP.
**Baseline:** `main` at `9d02179b`. Uncommitted WIP: `main.gd` (+116 lines), `bot_scenario_runner.gd`, `bot_wait_handlers.gd`, scenario JSON 12/78.
**Stats:** 159 `client/scripts/*.gd` files; `main.gd` 6,239 lines; 68 client tests; fog/hero-light domains wired in CODEMAP and smoke gates.
**Overview:** [`../20260625_v334-overview.md`](../20260625_v334-overview.md)

---

## Summary

v332–v334 client work is primarily stabilization and bot-routing repairs rather than large new presentation domains. Bot walk routing for open lab worlds and vendor hub bounds was fixed (`9d02179b`). Headless presentation guards and client-bot exit cleanup landed in prior fix commits. **The maintainability ratchet now fails on `main.gd`**: 6,239 lines exceeds the grandfathered baseline 6,132 + 25 allowance — this is the primary client risk.

In-progress wall-floor shader polish adds ~116 lines to `main.gd` (uncommitted), worsening the ratchet breach. The v331 recommendation to extract movement-intent orchestration remains the highest-value paydown seam.

---

## 1. Architecture

**[Strength]** Thin-client boundary preserved across v332–v334. Movement speed, ranged retreat timing, and room spawn placement are server-authoritative; client changes are presentation and bot harness only.

**[Strength]** Bot runner decomposition (`bot_scenario_runner.gd`, `bot_wait_handlers.gd`, facade pattern) keeps protocol bot steps out of `main.gd` for most flows. Recent WIP extends wait handlers for wall-floor scenario proof.

**[High]** `main.gd` at 6,239 lines is the single largest client liability. Ratchet failure:

```text
client/scripts/main.gd: 6239 lines exceeds grandfathered baseline 6132 + allowance 25
```

Uncommitted WIP adds another ~116 lines. Extraction is blocked until WIP lands or is stashed.

**[Med]** v331 carry-over: `_sync_discovery_minimap()` and similar one-liner tentacles from `main.gd` into downstream components remain. No movement-input presenter extraction yet.

---

## 2. Technical

**[Strength]** v331 orphan-test wiring holds — all smoke gates registered in `client_smoke.sh`.

**[Strength]** `fog_of_war_overlay.gd` baseline entry exists (624 lines, documented in `.maintainability/file-size-baseline.tsv`).

**[Med]** Wall-floor shader WIP touches `main.gd` presentation path — needs bot scenario 78 proof and headless guards before merge. Scenario JSON and wait-handler changes are coordinated in the dirty tree.

**[Low]** `HeroLightSource` recursive mesh walk still lacks depth limit (v331 note); acceptable for current asset pipeline.

---

## 3. Maintainability

**[High]** `main.gd` ratchet breach — CI `make maintainability` fails until lines are extracted or WIP is reverted. This is a hard gate for any slice finish on `main`.

**[Strength]** New focused files from v309–v331 remain appropriately sized (`hero_light_source.gd`, `discovery_minimap_state.gd`).

**[Med]** `fog_of_war_overlay.gd` at 624 lines is baselined with extraction note; no further growth this window.

---

## 4. Documentation

**[Strength]** CLAUDE.md spec-gate exemption documents presentation-only slice path (v322–v328 pattern).

**[Strength]** CODEMAP lists hero-light, fog, discovery minimap, and wall-floor bot scenario domains.

---

## Top 5 client refactors

1. **[blocker · Maint]** Extract movement-intent / lab-world presentation cluster from `main.gd` to lower below 6,157-line cap. Target `movement_input_presenter.gd` or wall-floor factory helper — must net-remove ≥82 lines from `main.gd`.

2. **[minor · Test]** Land wall-floor WIP with scenario 78 green and any new wait-handler coverage.

3. **[minor · Maint]** Route `_sync_discovery_minimap()` facing through a narrow sync context object to reduce `main.gd` tentacles.

4. **[future]** Leap-visual orchestration extraction (v308 carry-over) — larger seam, needs typed context.

5. **[reject]** Re-wire orphan smoke tests — resolved v331.

*Evidence: `make maintainability` output 2026-06-25; `git diff --stat main.gd`; `.maintainability/file-size-baseline.tsv`; `wc -l main.gd`; v331 client companion comparison.*
