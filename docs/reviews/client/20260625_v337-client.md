# arpg-dev — Client (Godot 4 / GDScript) review at slice **v337**

**Date:** 2026-06-25
**Scope:** Godot thin client — `client/scripts/`, `client/tests/`. Covers v335–v337 paydown plus v332–v334 stabilization carry-over.
**Baseline:** `main` at `da9a47b4`. Worktree clean.
**Stats:** 161 `client/scripts/*.gd` files; `main.gd` 6,143 lines; 71 client tests; `make maintainability` **PASS**.
**Overview:** [`../20260625_v337-overview.md`](../20260625_v337-overview.md)

---

## Summary

v335–v337 client work is primarily maintainability paydown rather than new presentation domains. **v335** extracted `movement_input_presenter.gd` (122 lines) from `main.gd`; **v336** landed wall-floor lab nav proof (scenario 78 + Go test); post-v334 `$refactor` extracted item-visual test probes and restored the maintainability ratchet. **`main.gd` now passes the file-size ratchet** (6,143 vs cap 6,152) but has only **9 lines of headroom** — the v334 blocker is resolved, fragility remains.

The thin-client boundary is preserved: movement speed, combat outcomes, and inventory mutations flow from server snapshots/deltas; client code handles input dispatch, local prediction, and presentation.

---

## 1. Architecture

**[Strength]** Thin-client contract stated at `main.gd:1-2` — server owns combat, loot, inventory, persistence.

**[Strength]** Movement speed from server `derived_stats` via `set_server_speed` on snapshot/delta apply — not client-tuned.

**[Strength]** `movement_input_presenter.gd` sends `move_intent` over WebSocket; `predicted_pos` is local prediction reconciled on server entity updates.

**[Strength]** Fog overlay (`fog_of_war_overlay.gd`) and discovery minimap (`discovery_minimap_state.gd`) are presentation/session-local only — no gameplay authority leak.

**[Strength]** Minimap sync is a thin delegate: `_sync_discovery_minimap()` → `DiscoveryMinimapState.sync_to_panel` (`main.gd:5796-5805`).

**[Strength]** Bot harness decomposed — `bot_wait_handlers.gd` (254 lines), `bot_scenario_runner.gd` facade; fog/wall assertions routed to focused handlers.

**[High]** `main.gd` at 6,143 lines remains the single largest client liability. Ratchet passes with only 9 lines headroom (baseline 6,127 + 25 = 6,152).

**[Med]** `equipped_update` delta path still uses direct `c["slot"]` at `main.gd:1106-1109` — inconsistent with `.get()` policy elsewhere; malformed delta could crash.

**[Low]** `HeroLightSource` recursive mesh walk lacks depth limit (v331 carry-over); acceptable for current asset pipeline.

---

## 2. Technical

**[Strength]** v331 orphan-test wiring holds — key gates registered in `client_smoke.sh` including `test_item_visuals.gd`, `test_movement_input_presenter.gd`, `test_fog_of_war_overlay.gd`, `test_discovery_minimap.gd`.

**[Strength]** Item-visual probe extraction (`item_visual_equipment_probe.gd`, `item_visual_interactable_probe.gd`, `item_visual_scale_probe.gd`) reduces `test_item_visuals.gd` to 461 lines (dropped grandfathered baseline).

**[Strength]** `godot --headless --path client --script res://tests/test_item_visuals.gd` — **PASSED** (post-refactor spot check).

**[Med]** `test_movement_input_presenter.gd` is thin — 4 assertions on static helpers; no mock `client.send` or keyboard-path coverage for v335 extraction.

**[Med]** v336 wall-floor scenario 78 proof landed; batch `make ci` still pending per PROGRESS.md (infra, not missing client tests).

**[Low]** `make client-unit` not re-run in this review (Godot path assumed available on dev host).

---

## 3. Maintainability

**[Strength]** `make maintainability` — **PASS**. `main.gd` ratchet breach from v334 **resolved**.

**[Med]** `main.gd` headroom: 6,143 / 6,152 cap — any +10-line slice re-fails CI without extraction.

**[Strength]** `test_item_visuals.gd` probe pattern is reusable — orchestrator + focused `RefCounted` probes; scale probe documents headless `await` deadlock hazard.

**[Med]** `fog_of_war_overlay.gd` at 624 lines — baselined with extraction note; at ceiling.

**[Low]** `bot_scenario_runner.gd` at 1,629 lines (grandfathered) — second-largest client file.

---

## 4. Documentation

**[Strength]** CODEMAP lists movement presenter, fog, discovery minimap, bot wait handlers.

**[Strength]** v335/v336 as-builts exist (`movement-input-presenter`, `wall-floor-lab-nav`).

**[Med]** v337 as-built is server-only — no client doc delta for this slice.

**[Low]** Last client review was v334 — this report supersedes `20260625_v334-client.md` for v335–v337 client work.

---

## Top 5 client refactors

1. **[blocker · Maint]** Extract attack-move / sticky-target / sustained-click cluster from `main.gd` — largest remaining input/combat seam; only 9 lines under ratchet cap.

2. **[minor · Maint · Test]** Move loot label visibility/color from `main.gd` into `loot_node_factory` or `loot_label_filter`; update `test_item_visuals` to probe factory directly (`_loot_label_color` already delegates to `_loot_factory`).

3. **[minor · Maint]** Extract fog hero-light / OmniLight3D lifecycle from `fog_of_war_overlay.gd` — baseline comment already calls for it.

4. **[minor · Arch · Test]** Harden `equipped_update` delta path with `c.get("slot", "")` + guard; extend `test_delta_apply` malformed case.

5. **[minor · Test]** Deepen `test_movement_input_presenter` — mock client stub for `try_send_keyboard_move`, `begin_force_stand`, `send_stop_intent`.

**[reject]** Re-wire orphan smoke tests — resolved v331.
**[reject]** Fix `main.gd` ratchet breach — resolved v335 + post-v334 refactor.

*Evidence: `make maintainability` 2026-06-25; `wc -l main.gd`; `.maintainability/file-size-baseline.tsv`; `client_smoke.sh`; v334 client review comparison.*
