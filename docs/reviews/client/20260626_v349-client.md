# arpg-dev — Client (Godot 4 / GDScript) review at slice **v349**

**Date:** 2026-06-26  
**Scope:** Godot thin client — v347–v349 presentation batch (render perf, forward_plus default, entity tick smoothing, smoke equip fix, orphan asset cleanup).  
**Baseline:** `main` @ `82422216`. Worktree: uncommitted `skills/review/SKILL.md` edit.  
**Stats:** 171 `client/scripts/*.gd`; `main.gd` 6,192 lines (20 under 6,212 cap); 70 `test_*.gd`; 66 `client_smoke.sh` gates; 8 orphan unit tests; 84 client bot scenarios in full matrix.  
**Overview:** [`../20260626_v349-overview.md`](../20260626_v349-overview.md)

---

## Summary

v348–v349 landed meaningful presentation infrastructure without crossing the authority boundary: **`forward_plus` for interactive play**, **`gl_compatibility` forced on headless automation** via `scripts/godot_ci_flags.sh`, and **data-driven remote-entity tick smoothing** (`movement_presentation.v0.json`, `entity_tick_smoothing*.gd`). Headless equip smoke was stabilized by syncing `weapon_sets` into `EquipmentVisualResolver` (`smoke.gd`).

`main.gd` remains the dominant liability (6,192 lines, only ~20 lines of ratchet headroom). All five v337 client refactor targets are still open. **`make ci-full` failed** 3/90 client-bot scenarios (`character_stats_panel`, `blacksmith_armor_recipe`, `wall_floor_dungeon_rollout`) while merge-gate pack scenarios passed.

## 1. Architecture

**[Strength]** Thin-client contract explicit — server owns combat, loot, inventory, persistence (`main.gd:1-2`).

**[Strength]** Delegation patterns maturing: `movement_input_presenter.gd`, `TownServiceBridge`, `InventoryTransferRouter`, `EquipmentVisualResolver`, v349 `entity_tick_smoothing_runtime.gd`.

**[High]** `main.gd` still god-coordinates 15+ panels, input clusters, loot labels, and UI refresh — largest remaining seam is attack-move / sticky-target / sustained-click (~2970–3028).

**[Med]** Local player intentionally **not** tick-smoothed — uses prediction + v299 anchor smoothing; scenario 84 documents this, but v349 spec AC still mentions local anchor interpolation (spec/implementation drift).

## 2. Technical

**[Strength]** v348 renderer split is clean:

```28:30:client/project.godot
[rendering]

renderer/rendering_method="forward_plus"
```

Headless paths source `GODOT_HEADLESS_FLAGS="--headless --rendering-method gl_compatibility"` (`scripts/godot_ci_flags.sh`).

**[Strength]** v349 remote entity smoothing wired in `_process` and `_upsert_entity` (`main.gd:935-936`, `1648-1649`); unit test registered in smoke.

**[Strength]** `5dc2706c` smoke fix: `weapon_set_update` + `_sync_resolver_from_server_state()` — resolves CI flake where visual mount failed despite authoritative equip.

**[Med]** `equipped_update` still uses bare `c["slot"]` at `main.gd:1129` — v337 hardening item open.

**[Med]** `make ci-full` client-bot failures:
- `character_stats_panel` — `wait_event` timeout at step 4 (1.0s budget).
- `blacksmith_armor_recipe` — `wait_blacksmith_panel` timeout at step 35 (15s).
- `wall_floor_dungeon_rollout` — `wait_wall_layout` timeout; stayed on level 0 with zero generated walls (expected level -1).

## 3. Maintainability

**[Strength]** Ratchet pass: `main.gd` 6,192 / cap 6,212 (+20 headroom after v347 baseline bump).

**[Med]** `fog_of_war_overlay.gd` at 644-line baseline cap; hero `OmniLight3D` lifecycle still inline.

**[Med]** `bot_scenario_runner.gd` at grandfathered ceiling (~1,654 lines).

**[High]** **8 orphan GDScript tests** not registered in `client_smoke.sh` (market board/search/sort/comparison/badges, bishop panel, material wallet, quest giver visual, elite objective minimap) — zero CI coverage despite files on disk.

## 4. Documentation

**[Strength]** v348/v349 full spec + plan + as-built trail.

**[Med]** v343/v344/v346 presentation slices lack specs; lifecycle rows for v340–v347 still show missing plan links.

## Top 5 client refactors

1. **[blocker · Maint]** Extract attack-move / sticky-target / sustained-click cluster from `main.gd` — ~20 lines of ratchet headroom left.
2. **[minor · Test]** Register 8 orphan market/bishop/material tests in `client_smoke.sh`.
3. **[minor · Arch]** Resolve local-player tick-smoothing spec drift — wire player path or narrow spec to match scenario 84.
4. **[minor · Maint · Test]** Harden `equipped_update` with `c.get("slot", "")` + malformed-delta unit test.
5. **[blocker · Test]** Fix or stabilize `wall_floor_dungeon_rollout`, `character_stats_panel`, and `blacksmith_armor_recipe` extended client-bot scenarios (ci-full failures).

*Evidence: `client_smoke.sh`, `godot_ci_flags.sh`, `docs/as-built/v348_*`, `docs/as-built/v349_*`, git `5dc2706c`, `/tmp/arpg-ci-full-review.log`.*
