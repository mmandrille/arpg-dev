# arpg-dev — Client (Godot 4 / GDScript) review at slice **v384**

**Date:** 2026-06-29  
**Scope:** Godot thin client — v370–v384 combat/exploration perf batch vs v349 baseline.  
**Baseline:** `main` @ `625f5600`. Worktree: **clean**.  
**Stats:** 188 `client/scripts/*.gd`; `main.gd` **6,366** lines (baseline 6,361, ~20 lines headroom); 82 test files / **74** smoke gates; **8 orphan** unit tests; `fog_of_war_overlay.gd` 611 lines (down from 644).  
**Overview:** [`../20260629_v384-overview.md`](../20260629_v384-overview.md)

---

## Summary

The thin-client boundary remains intact: presentation-only perf degradations (LOD caps, projectile visibility cull, fog shader throttle, delta coalescing, recon backpressure) do not cross the authoritative wire. v370 added phase-level `[client-perf]` attribution; v372–v378 landed as extracted modules wired from `main.gd`.

`main.gd` grew **+174 lines** since v349 despite extractions — attack-move / sticky / sustained cluster (~3082–3200) is still unextracted with ~20 lines of ratchet headroom. Eight market/bishop/material orphan unit tests remain unregistered. v372–v378 as-builts are stubs; perf logic is mostly bot-probed (`crowded_melee_perf_probe`) rather than unit-locked.

## 1. Architecture

**[Strength]** Contract explicit in coordinator header (`client/scripts/main.gd:1–2`) — thin renderer; server owns combat, loot, inventory.

**[Strength]** Transport intent-only (`client/scripts/net_client.gd:1`); outcomes via WebSocket deltas/snapshots only.

**[Strength]** Perf/degrade paths are presentation-only — LOD hides geometry; projectile `visible` culling; fog shader throttle; recon backpressure clears client pending targets.

**[Strength]** Data-driven tuning in `shared/rules/main_config.v0.json` (`presentation_lod`, `client_perf`, `loot_labels`) and `shared/assets/fog_presentation.v0.json` (`combat_crowd_shader_throttle`).

**[Med]** `equipped_update` still uses bare `c["slot"]` (`main.gd:1213–1216`) — v349 carry-over; malformed delta risk.

**[Low]** Delta coalesce last-wins on `performance` payload (`main.gd:1021–1027`).

## 2. Technical

**[Strength]** v370 `PerfPhaseTimer` — `measure_usec("net_poll"|"delta"|"entities")`; unit test `test_perf_phase_timer.gd` in smoke.

**[Strength]** v372 fog combat throttle — `fog_of_war_overlay.gd` `_shader_update_throttled()`; `set_live_monster_count` from perf status (`main.gd:983–985`); partial test in `test_fog_of_war_overlay.gd`.

**[Strength]** v373/v375 presentation caps — `entity_presentation_lod.gd`, `projectile_presentation_cap.gd` wired in `_process` and `_upsert_entity`.

**[Strength]** v377 delta frame coalescing — `_pending_delta_payloads`, `_flush_pending_deltas()` queue on `state_delta` (`main.gd:998`), flush before poll (`main.gd:943`).

**[Strength]** v378 recon backpressure — `_apply_reconciliation_backpressure()` on reconcile (`main.gd:1706–1707`); clears pending targets + charge visual (`main.gd:3447–3452`).

**[Med]** No focused unit tests for LOD, projectile cap, delta coalesce, recon backpressure — crowded-fight behavior under cap/throttle not CI-locked at unit level.

**[Med]** `main.gd` attack-move cluster unextracted (~3082–3200): `_defer_monster_click`, `_start_attack_move`, `_tick_sustained_click`, sticky/sustained vars (`main.gd:271–273`).

## 3. Maintainability

**[Strength]** Ratchet at ceiling but green: baseline 6,361, actual 6,366 (~20 lines before +25 breach at 6,386).

**[Strength]** `fog_of_war_overlay.gd` shrank to 611 lines (v372).

**[Med]** Panel god-files persist: `inventory_panel.gd` 1,556; `shop_panel.gd` 1,217; `market_panel.gd` 1,059 — parallel `main.gd` debt.

**[Med]** 8 orphan unit tests unchanged since v349 (market/bishop/material/quest domains).

**[Strength]** +8 smoke gates since v349 (presentation tests registered in `client_smoke.sh` section 2o).

## 4. Documentation

**[Strength]** v370 as-built is substantive (`docs/as-built/v370_client-perf-breakdown.md`).

**[Med]** v372–v378 as-builts are one-liner stubs — no behavior contracts or verification commands.

**[Low]** `docs/progress/scenario-catalog.md` should note `crowded_melee_perf_probe` pack membership if promoted.

## Top 5 client refactors

1. **[blocker · Maint]** Extract attack-move / sticky / sustained cluster from `main.gd` (~3082–3200) — ratchet headroom ~20 lines.

2. **[minor · Test]** Register **8 orphan** market/bishop/material/quest tests in `scripts/client_smoke.sh`.

3. **[minor · Test]** Add focused tests for `entity_presentation_lod.gd`, `projectile_presentation_cap.gd`, `_flush_pending_deltas`, `_apply_reconciliation_backpressure`.

4. **[minor · Maint]** Harden `equipped_update` → `c.get("slot", "")` + malformed-delta test.

5. **[minor · SDD]** Backfill v372–v378 as-builts with behavior + verification commands (v370 is the template).

*Evidence: `client/scripts/main.gd`, `perf_phase_timer.gd`, `entity_presentation_lod.gd`, `projectile_presentation_cap.gd`, `fog_of_war_overlay.gd`, `scripts/client_smoke.sh`, `.maintainability/file-size-baseline.tsv`.*
