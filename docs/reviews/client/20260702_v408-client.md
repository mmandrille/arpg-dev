# arpg-dev — Client (Godot 4 / GDScript) review at slice **v408**

**Date:** 2026-07-02  
**Scope:** Godot thin client — v399–v408 autoloop batch vs v398 baseline.  
**Baseline:** `main` @ `ffdd3f0b`. Worktree: **clean** at review start.  
**Stats:** `main.gd` **6,524** lines (baseline 6,523, +1); **98** `client_smoke.sh` gates; **102** client-bot scenarios; **3** orphan visual probes.  
**Overview:** [`../20260702_v408-overview.md`](../20260702_v408-overview.md)

---

## Summary

The Godot client remains a credible thin renderer: combat/loot/inventory outcomes arrive via authoritative deltas; UI sends intents and never owns roll logic. **v399 closed the biggest v398 gap** — extended bot `ws_reconnect_proof` plus `test_connection_recovery_runtime.gd` exercise reconnect overlay/resync.

**Regression at review start:** `character_stats_panel.gd` reached **728 lines** (not grandfathered), causing **`make maintainability` / `make ci-full` step 1 failure**. Dual-wield column work (v311-era) grew the panel without extraction. **`$refactor` split breakdown logic into `character_stats_breakdown.gd`** (547 + 216 lines) — ratchet green post-fix.

v407–v408 elemental presentation is event-driven (`damage_type_combat_text.gd`); no client proc module needed.

## 1. Architecture

**[Strength]** Thin-client contract at `main.gd:1–2`; stat allocation intent-only (`main.gd:4101–4104`).

**[Strength]** v384 attack-move extraction held — `attack_move_input_coordinator.gd` (133 lines); reach in `combat_reach.gd` (122 lines).

**[Strength]** v398/v399 reconnect authority preserved — pending state cleared on loss, recovery tick in `_process`, delta flush suppressed while active; v399 bot proof bypasses `bot_mode` when opt-in (`connection_recovery_runtime.gd:66–67`).

**[Med]** `main.gd` remains **6,524-line** coordinator — `_apply_delta` op dispatch + event fan-out colocated (`main.gd:1277–1419+`).

**[Med]** `CombatReach` duplicates server reach rules for client-side prediction (`combat_reach.gd:48–62`) — drift risk without shared golden parity.

## 2. Technical

**[Strength]** v399 closed reconnect proof gap — `ws_reconnect_proof.json`, `bot_reconnect_proof_actions.gd`, `test_connection_recovery_runtime.gd` (`client_smoke.sh:218`).

**[Strength]** v407–v408 elemental hits via `damage_type_combat_text.gd:23–50` — event-driven, not client-computed.

**[High]** **`character_stats_panel.gd` ratchet breach** at review start (728 lines) — fixed in `$refactor` via `character_stats_breakdown.gd` extraction.

**[Med]** v407 item summaries server-authored; no focused client unit test for elemental summary tooltip rendering.

**[Med]** `bot_scenario_runner.gd` **1,678** lines (baseline 1,663, +15).

## 3. Maintainability

| File | v408 (pre-refactor) | Post-refactor | Baseline |
|------|---------------------|---------------|----------|
| `character_stats_panel.gd` | 728 (**FAIL**) | **547** | none (new-file target 600) |
| `main.gd` | 6,524 | 6,524 | 6,523 (+1) |
| `inventory_panel.gd` | 1,536 | 1,536 | 1,516 (+20) |
| `shop_panel.gd` | 1,247 | 1,247 | 1,243 (+4) |
| `stash_panel.gd` | 1,180 | 1,180 | 1,155 (+25, at cap) |

**[Strength]** Orphan visual probes still **3** only (manual-only).

## 4. Documentation

**[Strength]** v399–v408 as-builts substantive with verification commands.

**[Strength]** CODEMAP lists reconnect domain and stats panel modules.

**[Med]** v407/v408 as-builts server-centric — thin client presentation path undocumented.

**[Low]** Typed `ConnectionRecoveryContext` for 12-arg `tick()` still open (`connection_recovery_runtime.gd:46–65`).

---

## Top 5 client refactors

1. **[minor · Maint]** ~~Split `character_stats_panel.gd`~~ — **done in `$refactor`** → `character_stats_breakdown.gd`.
2. **[High · Maint]** Extract next `main.gd` cluster — `_apply_delta` event loop or `_upsert_entity` into importable `RefCounted` modules.
3. **[High · Maint]** Decompose `inventory_panel.gd` (1,536 lines) — v398 #2 still open.
4. **[Med · Test]** Client unit test for elemental item summary tooltip lines (v407 server summaries).
5. **[Med · Maint]** Typed `ConnectionRecoveryContext` — collapse 12-callable `ConnectionRecoveryRuntime.tick()` signature.

*Evidence: file reads at `ffdd3f0b`; comparison to [`20260701_v398-client.md`](20260701_v398-client.md).*
