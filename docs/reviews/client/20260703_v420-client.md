# arpg-dev — Client (Godot 4 / GDScript) review at slice **v420**

**Date:** 2026-07-03  
**Scope:** Godot thin client — v409–v420 autoloop batch vs v408 baseline.  
**Baseline:** `main` @ `dd037a16`. Worktree: **clean**.  
**Stats:** 239 scripts under `client/scripts/`; **106** `test_*.gd` gates; **103** client-bot scenarios; **100** `client_smoke.sh` gates.  
**Overview:** [`../20260703_v420-overview.md`](../20260703_v420-overview.md)

---

## Summary

The Godot client remains a credible thin renderer: panels emit intents; outcomes arrive via snapshots/deltas; presentation modules own animation, tooltips, and combat text. v408 pain points — **13 extended client-bot failures** and **`character_stats_panel.gd` ratchet breach** — are **closed** at the v420 baseline (`make ci-full` green). Recent fixes aligned mercenary default **Defend** stance and **rank-10** skill progression assertions (`ca2d9567`, `78c1f418`).

**[Strength]** v411 skill-tree layout and v409 training-damage-log extractions improved cohesion. **Risks** unchanged: `main.gd` **6,539** lines (+15 vs v408); panel god-files (`inventory_panel.gd` 1,527; `shop_panel.gd` 1,247; `market_panel.gd` 1,081).

## 1. Architecture

**[Strength]** Explicit thin-client contract at `main.gd:1–2`; stat/skill allocation and mercenary hire are intent-only (`main.gd:4108–4116`, `:845–853`).

**[Strength]** Event-driven combat presentation — `damage_type_combat_text.gd` renders server `damage` / `damage_type` events, not client rolls.

**[Strength]** v399 reconnect stack clears pending input and resyncs from snapshot (`connection_recovery_runtime.gd`).

**[Med]** Client-side combat reach mirror in `combat_reach.gd:48–57` for click-to-attack UX — drift risk without shared golden lock.

**[Med]** Blacksmith / mystery-shop previews mirror shared rules for labels — acceptable preview boundary with unit tests.

## 2. Technical

**[Strength]** `make ci-full` client bot **103/103 PASS** at v420 baseline (was 13 failures at v408 review).

**[Strength]** Mercenary panel default stance matches server (`mercenary_panel.gd:22` — `defend`); bot scenarios updated (`56_mercenary_stats_card.json`, `64_mercenary_combat_stats.json`, `69_mercenary_offer_variant_ui.json`).

**[Strength]** v417 rank scaling mirrored in `skill_rank_scaling.gd` with unit tests; v418 synergy tooltips render server `synergy_status`.

**[Med]** `test_coop_client.gd` still fixtures `max_rank: 5` in multiple places — stale vs rank-10 rules.

**[Low]** v420 build-branch skills have extended protocol proof only — no new client-bot scenario (appropriate for server-heavy slice).

## 3. Maintainability

| File | v408 | v420 | Δ |
|------|------|------|---|
| `main.gd` | 6,524 | 6,539 | +15 |
| `inventory_panel.gd` | 1,536 | 1,527 | −9 |
| `shop_panel.gd` | 1,247 | 1,247 | 0 |
| `stash_panel.gd` | 1,180 | 1,180 | 0 |
| `character_stats_panel.gd` | 547 | 547 | stable |
| `character_stats_breakdown.gd` | 216 | 216 | v408 extraction win |
| `bot_scenario_runner.gd` | 1,678 | 1,677 | −1 |

**[High]** `_apply_delta` op-dispatch hub still monolithic (`main.gd:1281+`).

## 4. Documentation

**[Strength]** v409–v420 as-builts document presentation-only vs coordinated protocol touches.

**[Med]** Client wiring for v407–v408 elemental presentation paths remains thinner in as-builts than server proof.

---

## Top 5 client refactors

1. **[High · Maint]** Extract `main.gd` delta/event cluster — `_apply_delta` op dispatch + event fan-out into importable modules (e.g. `delta_apply_coordinator.gd`).
2. **[High · Maint]** Decompose `client/scripts/inventory_panel.gd` (1,527 lines) — paper-doll, drag/drop, tooltip sections.
3. **[Med · Maint]** Split `client/scripts/bot_scenario_runner.gd` (1,677 lines) — continue assertion-domain extraction via `bot_step_catalog.gd`.
4. **[Med · Test]** Refresh `client/tests/test_coop_client.gd` — replace hardcoded `max_rank: 5` with rule-derived or rank-10 values.
5. **[Med · Maint]** Typed `ConnectionRecoveryContext` for `connection_recovery_runtime.gd` — collapse wide `tick()` callable surface.

*Evidence: file reads at `dd037a16`; comparison to [`20260702_v408-client.md`](20260702_v408-client.md).*
