# arpg-dev — Client (Godot 4 / GDScript) review at slice **v460**

**Date:** 2026-07-08
**Scope:** Godot thin client — v449–v460 (status effects bar, inventory wallet delta runtime, crosshair target system, live transport stability proof, leveled potion presentation) vs v448 baseline.
**Baseline:** `main` @ `e75e64d0`. v448 baseline: `ebcbdc9a`. Worktree: **clean**.
**Stats:** ~250 scripts under `client/scripts/`; **110** `test_*.gd` gates; client-bot pack stable at 13 scenarios; `main.gd` **6,711** lines; `inventory_panel.gd` **1,657** lines; `bot_controller.gd` **848** lines (headroom **0**).
**Overview:** [`../20260708_v460-overview.md`](../20260708_v460-overview.md)

---

## Summary

v449–v460 delivered genuine structural wins alongside accelerating coordinator pressure. **`InventoryWalletDeltaRuntimeScript`** (197 lines) correctly extracted inventory/stash/resource-bag change-op handling from `_apply_delta` with an extraction-independence test. **`StatusEffectsBar`** (162 lines) and **`CrosshairTargetSystem`** (242 lines) are reference node-component / RefCounted patterns. **v457** established the `runner: godot_client` + zero-reconnect live transport proof template.

Counterweights: `_apply_delta` event fan-out grew to **472 lines** with ~69 `if event_type ==` branches (`main.gd:1293–1764`). `bot_controller.gd` hit its ratchet ceiling (848/823, headroom 0). `PotionIconLabel.is_leveled_potion()` hardcodes three potion IDs instead of reading rules data — a new data-driven policy violation.

---

## 1. Architecture

**[Strength]** `inventory_wallet_delta_runtime.gd:1–197` — focused extraction with `test_inventory_wallet_delta_runtime.gd` proving direct import without `MainScript`.

**[Strength]** `status_effects_bar.gd:1–162` — self-contained presenter with tick decay, no protocol knowledge; registered in `client_smoke.sh`.

**[Strength]** `crosshair_target_system.gd:1–242` — typed `CrosshairTargetContext`, clean API, 314-line unit test.

**[Strength]** `99_ranger_volley_live_session_stability.json` — first live Godot transport topology proof (`reconnect_count_equals: 0`).

**[High]** `_apply_delta` events loop (`main.gd:1293–1764`, 472 lines, ~69 branches). Changes loop delegates to wallet runtime, but events loop still absorbs every new event type as flat `if/continue`. Protocol rename or ordering bug silently breaks multiple features.

**[High]** `bot_controller.gd` at ratchet ceiling (848 lines, headroom 0). v460 `use_hotbar_slot` addition blocks the next bot action without mandatory extraction.

**[Med]** `PotionIconLabel.is_leveled_potion()` (`potion_icon_label.gd:7–12`) hardcodes `"red_potion"`, `"blue_potion"`, `"rejuv_potion"`. Server/shop uses `leveled_consumable: true` in `shops.v0.json`. New leveled consumables won't show level labels without code change.

**[Med]** `_steward_hunt_state: Dictionary` still untyped at `main.gd:285` (flagged v448, unchanged).

**[Low]** `inventory_wallet_delta_runtime.gd:91` prefers dead `bag_item_id` key over canonical `stash_item_id` for `resource_bag_item_remove`.

---

## 2. Technical

**[Med]** Data-driven policy violation in `potion_icon_label.gd` — should read item/shop rules for `leveled_consumable` flag (or promote flag to `items.v0.json`).

**[Open]** `quest_steward_pick_intent` end-to-end Godot client CI proof still deferred (v448 → v460).

**[Low]** `skill_ground_aim_lab.json` is protocol-only; `CrosshairTargetSystem` direction cast path has no live Godot CI proof.

---

## 3. Maintainability

| File | v448 actual | v460 actual | Baseline | Headroom | Status |
|---|---|---|---|---|---|
| `main.gd` | 6,625 | 6,711 | 6,725 | 14 | +86 since v448 |
| `inventory_panel.gd` | 1,527 | 1,657 | 1,656 | 24 | +130 since v448 |
| `bot_controller.gd` | 823 | **848** | 823 | **0** | **AT CEILING** |
| `stash_panel.gd` | 1,180 | 1,194 | 1,194 | 25 | Stable |
| `market_panel.gd` | 1,081 | 1,081 | 1,059 | 3 | Near ceiling |
| `blacksmith_panel.gd` | — | 793 | 771 | 3 | Near ceiling |
| `skills_panel.gd` | — | 999 | 1,010 | — | **Stale baseline +11** |

---

## 4. Documentation

**[Med]** `skills_panel.gd` baseline (1010) is 11 lines above actual (999) — should be lowered per ratchet rules.

**[Med]** v460 as-built defers `test_potion_icon_label.gd` without flagging the data-driven policy gap.

**[Low]** `main.gd` bottom 500+ lines still lack section headers (unchanged since v448).

---

## Top 5 client refactors

1. **[High · Maint]** Extract `_apply_delta` event fan-out into `DeltaEventCoordinator` (combat, status, skills, quest, service-panels domains).

2. **[Med · Correctness]** Fix `PotionIconLabel.is_leveled_potion()` to read rules; add `test_potion_icon_label.gd` + `client_smoke.sh` registration.

3. **[High · Maint]** Extract `bot_controller.gd` hotbar/inventory action group (~80 lines) to `bot_inventory_actions.gd` before next slice.

4. **[Med · Test]** Expand `test_inventory_wallet_delta_runtime.gd` to cover resource-bag add/remove/upsert/snapshot paths.

5. **[Minor · Maint]** Lower `skills_panel.gd` baseline to 999; pre-empt near-ceiling files (`market_panel.gd`, `blacksmith_panel.gd`).

*Evidence: subagent client audit + `potion_icon_label.gd`, `.maintainability/file-size-baseline.tsv`, `main.gd:1293`.*
