{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://arpg.dev/schemas/assets/class_presentations.v0.schema.json",
  "title": "Class presentations (v0)",
  "description": "Client-facing class labels, icons, and character models. Gameplay authority stays in shared/rules/character_progression.v0.json.",
  "type": "object",
  "required": ["version", "classes"],
  "additionalProperties": false,
  "properties": {
    "version": { "const": 0 },
    "classes": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["icon", "model"],
        "additionalProperties": false,
        "properties": {
          "icon": {
            "type": "object",
            "required": ["label", "shape", "color", "accent"],
            "additionalProperties": false,
            "properties": {
              "label": { "type": "string", "minLength": 1, "maxLength": 3 },
              "shape": { "type": "string", "enum": ["axe", "spark", "shield", "dagger", "bow"] },
              "color": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" },
              "accent": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
            }
          },
          "model": {
            "type": "object",
            "required": ["asset_id"],
            "additionalProperties": false,
            "properties": {
              "asset_id": { "type": "string", "minLength": 1 },
              "scale": { "type": "number", "exclusiveMinimum": 0 },
              "height_offset": { "type": "number" }
            }
          },
          "idle_stance": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "scale": { "type": "number", "exclusiveMinimum": 0 },
              "lean_degrees": { "type": "number" }
            }
          }
        }
      }
    }
  }
}
